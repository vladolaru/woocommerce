---
session: 2026-06-15-core-native-payments
type: review
by: codex
created: 2026-06-17 15:54
last_updated: 2026-06-21 03:42
tool: spec-conformance-baseline
target: accumulated A1-A3/B0-B2 native payments and multi-currency runtime
reconciles:
  - analysis-supervisor-realignment.md
  - review-agent-findings.md
  - review-a4as-admin-readiness-decision.md
  - review-a4as-code-review.md
  - review-a5g-rollout-multisite.md
  - review-a4bd-accumulated-gate.md
  - review-a5h-cutover-readiness-refresh.md
  - review-post-a5h-next-slice-scan.md
  - staging-log.md
status: draft
---

# Retroactive Spec-Conformance Baseline

> **Prompt:** "One addition to the supervisor instructions shared earlier:
>
> N6 — Run a one-time retroactive spec-conformance baseline before starting A4/A5.
>
>   The forward stage-boundary gate (N5) is the right standing process, but A1–A3 and B0–B2 are already
>   declared "done" without ever passing it — the H1–H12 backfill remediated A3 reactively, not against a
>   conformance checklist. Before A4/A5 begins, run the N5 gate once, retroactively, over the accumulated
>   payments + multi-currency runtime: check it against design-spec.md, every touched-surface bucket
>   disposition in bc-manifest.md, and the implementation-plan §0.6 invariants. Record the result in
>   staging-log.md as the spec-conformance baseline.
>
>   This is not re-doing the work — keep it at the contract/architecture level, not a line-by-line re-review.
>   The point is to establish a known-good line so every later stage gate diffs against an audited baseline
>   instead of an assumed one. Fail-closed: anything it surfaces becomes a tracked slice (H## or its true
>   canonical stage) before A4/A5 proceed. Verify any finding against source before acting on it."

> **Follow-up prompt:** "N7 — Close the verification-coverage gaps the baseline exposed (harden the gates, not just the code)."

## Method

This baseline is a contract-level review over the accumulated branch state after H13 (`399ccd3790`). It checks the already-declared A1-A3 and B0-B2 work against the canonical architecture, touched backward-compatibility buckets, and implementation-plan §0.6 invariants. It is not a line-by-line code review and does not replace later focused implementation gates. N7 expands the gate before A4/A5 by requiring the financial, Bucket-C Subscriptions, perf, and bundle-size coverage gaps to be hardened and exercised, not merely tracked.

Inputs to read before verdict: `~/Work/a8c/ai-prompts/goals/woopayments-merge/design-spec.md`, `~/Work/a8c/ai-prompts/goals/woopayments-merge/follow-up/bc-manifest.md`, `~/Work/a8c/ai-prompts/goals/woopayments-merge/follow-up/implementation-plan.md` §0.6 plus relevant stage gates, the local `staging-log.md`, and source probes for any potential finding.

## Working Checklist

- Architecture: one native payments runtime, provider adapters keep provider-specific behavior behind neutral interfaces, standalone WooPayments plugin wins when active, no global `WC_Payments` compatibility shim is introduced.
- Payments surfaces touched through A3/H12: checkout card and WooPay shopper surfaces preserve functional and stylistic parity, including test-mode shopper copy, Elements appearance/fonts, WooPay script/style gating, saved tokens, account readiness, and restored harness/browser evidence.
- Multi-currency surfaces touched through B2: read-only state, live hooks, switcher, REST/settings projections, order/cart/checkout/account compatibility, extension compatibility, Bucket-E option/meta keys, and provider-backed state honor the manifest dispositions.
- BC manifest: every touched surface bucket is explicitly preserved, aliased, deprecated, or dropped; no touched surface remains implicitly assumed.
- Invariants: no money movement without verification, Bucket-E byte-identical storage where promised, Bucket-C integrations intact, per-surface performance preserved, fail-closed behavior for unknown or incomplete runtime/cutover state.

## Findings

### Provisional Verdict

BLOCKED for A4/A5 progression until the tracked slices below are either completed or explicitly accepted as true-stage work with fail-closed guards. H13 means mandatory cutover is currently fail-closed, so these gaps are not silently merchant-facing through auto-deactivation; that guardrail is working as intended. N7 also makes the previous verification-coverage caveats blocking before A4/A5 because the prior "financial reconciliation passed" evidence is refunds-only and the perf/bundle gates were not actually exercised across the full touched surface matrix.

### Source-Verified Blockers

1. H14 / canonical A3: non-WooPay express checkout is not yet covered by native Core. The A3 spec says classic, Blocks, express, and WooPay drive generic processing through `ProviderContract`, and the checkout behavioral contract explicitly includes express buttons. Native source currently exposes WooPay express config with `enabled_methods` limited to `woopay` when the WooPay context setting is enabled, while the reference WooPayments client has a distinct Express Checkout Element path for Apple Pay / Google Pay (`payment_request`) and Amazon Pay (`amazon_pay`). Evidence: implementation-plan.md lines 115-120; design-spec.md lines 139-145; native `WooPaymentsWooPaySessionService::get_express_checkout_params()` and `is_woopay_express_checkout_enabled_at()`; reference `includes/express-checkout/class-wc-payments-express-checkout-button-helper.php` lines 234-365 and `client/express-checkout/utils/button-style-settings.ts` lines 43-52. This must be tracked as its own A3-family slice, not hidden inside WooPay.

2. H15/H31 / canonical A2/A5: provider event disposition is improved but the broader A2/A5 baseline remains blocked by adjacent cutover reliability work. A2 requires webhook-to-order-effect parity for every event type from the extraction list, and A5 requires route/event continuity across plugin deactivation. At the original baseline, native `WooPaymentsEventIngestor::KNOWN_UNHANDLED_EVENT_TYPES` included account, dispute, refund, invoice, and notification events and threw for them in `build_lifecycle_event()`. Status update: H25 removed and handled the `charge.dispute.*` group, H29 removed and handled `charge.refunded` plus `charge.refund.updated`, H30 removed and handled `account.updated`, `account.deleted`, and `wcpay.notification`, and H31 retired `invoice.paid`, `invoice.payment_failed`, and `invoice.upcoming` as Bucket D Stripe Billing events behind a hard legacy-marker cutover guard plus loud webhook alarms. The provider-event list is no longer the active blocker, but A2/A5 still cannot be baseline-green until queue readiness, webhook observability, and admin/cutover gates are closed. Evidence: implementation-plan.md lines 106-113 and 133-143; design-spec.md lines 377-380; `WooPaymentsEventIngestor.php`; `WooPaymentsCutoverController.php`; H25/H29/H30/H31 entries in `staging-log.md`.

3. N7a / canonical A3 verification: the financial reconciliation matrix is incomplete and prior package entries overstated the breadth of the gate. The spec requires a Stage-3+ test-mode matrix covering charge amounts, captures/authorizations, full/partial refunds, dispute outcomes, payouts, fees, and multi-currency. The restored deterministic harness explicitly states that `financial-reconcile.sh` covers refunds only, not charge amount, captures/auths, disputes, payouts, fees, or multi-currency; `verify.sh` also reports deterministic PASS while the financial gate has only refunds-only coverage. Evidence: design-spec.md lines 525-534; implementation-plan.md lines 44-49 and 115-120; `tools/woopayments-merge/HARNESS.md` lines 98-115; `tools/woopayments-merge/financial-reconcile.sh` header lines 5-7; `tools/woopayments-merge/verify.sh` financial gate labels. This is a baseline verification blocker and is now the active N7 first implementation slice.

4. A4 / true canonical A4: merchant admin surfaces are not implemented yet beyond onboarding/settings entry points. The A4 spec expects the provider admin app on the Core build, all screens rendering and functioning, bundle-size reduction, and a single `@wordpress/data` registry. Current native Core only has `client/admin/client/woopayments/onboarding/index.ts` re-exporting the Settings Payments onboarding provider, while the reference plugin has full overview, transactions, disputes, deposits/payouts, reports, payment details, and card-reader surfaces. H13 correctly blocks A5 through `native_admin_surfaces_unavailable`; A4 itself still needs a true A4 implementation slice rather than an H-label backfill. Evidence: implementation-plan.md lines 124-131; design-spec.md lines 141-145; native file listing under `plugins/woocommerce/client/admin/client/woopayments`; `WooPaymentsCutoverController.php` lines 309-318.

5. B0/B1 / true canonical multi-currency baseline: multi-currency standalone-domain conformance is blocked by concrete WooPayments imports in generic `Internal\MultiCurrency` code. `MultiCurrencyAdminNoteController` imports and injects `WooPaymentsProvider`, and `CurrencyRateProviderRegistryFactory` imports and injects `WooPaymentsCurrencyRateProviderRegistrar`. This contradicts the B0/B1 gate that the multi-currency domain must operate independently with manual rates and no payments provider. Evidence: implementation-plan.md lines 157-176; bc-manifest.md lines 27-31 and 92-96; `plugins/woocommerce/src/Internal/MultiCurrency/MultiCurrencyAdminNoteController.php` lines 13 and 87-92; `plugins/woocommerce/src/Internal/MultiCurrency/Providers/CurrencyRateProviderRegistryFactory.php` lines 10 and 32-35; Hypatia architecture review in `review-agent-findings.md`.

6. A2/A5 / queue handoff: native cutover has no queue-readiness preflight, and two manifest-listed operational hooks are deliberately unregistered today. `WooPaymentsOperationalQueueService::register()` omits `wcpay_instant_deposit_reminder` and `wcpay_post_kyc_activation_email_send`, and `WooPaymentsOperationalQueueServiceTest` asserts both are absent; the reference plugin registers them. H13 does not yet block on queue completeness. Evidence: design-spec.md lines 377-378; `tools/woopayments-merge/queue-handoff-manifest.json`; native `WooPaymentsOperationalQueueService.php` lines 121-128; native `WooPaymentsOperationalQueueServiceTest.php` lines 59-60; reference `class-wc-payments-account.php` lines 135-138; reference `class-wc-payments-post-kyc-activation-email-service.php` lines 74-78 and 113; Fermat and Mill reviews in `review-agent-findings.md`.

7. A2/A5 / webhook observability: native webhook failures return 400/500 without local logging while the reference logs invalid-webhook and generic processing exceptions. This weakens provider-event diagnosis at exactly the event-continuity boundary. Evidence: native `WooPaymentsWebhookRestController.php` lines 96-103; reference `includes/admin/class-wc-rest-payments-webhook-controller.php` lines 79-86; Fermat review in `review-agent-findings.md`.

8. A3/H12 / Tracks continuity: surviving native checkout/WooPay surfaces currently drop reference Tracks emitters. Native WooPay and checkout assets lack `recordEvent`/`recordUserEvent` calls, while the reference plugin emits `woopay_button_load`, `woopay_button_click`, `checkout_email_address_woopay_check`, `checkout_woopay_save_my_info_offered`, `checkout_save_my_info_click`, `woopay_skipped`, `checkout_place_order_button_click`, and Apple Pay/Google Pay express load/click events from surviving checkout/express surfaces. Evidence: native `plugins/woocommerce/client/blocks/assets/js/extensions/payment-methods/woopayments/woopay/index.js` click/init path; native `rg` only found the non-Tracks place-order hook in `woopayments-checkout.js`; reference `client/checkout` and `client/express-checkout` emitters; Mill review in `review-agent-findings.md`.

9. N7b/N7c / canonical Bucket-C and perf/bundle gates: Bucket-C WC Subscriptions renewal conformance has not been exercised end-to-end against native, and perf/bundle-size evidence remains a runbook/judgment item rather than measured stage-gate output. Evidence: implementation-plan.md lines 47-49, 64-67, 120, 127-129, 200-201; design-spec.md lines 477-494 and 532-534; `tools/woopayments-merge/HARNESS.md` lines 104-115. N7 requires these gates to be widened and rerun before A4/A5.

### Passes/Covered Baseline Claims

- One-runtime cutover safety is structurally guarded: native payments are arbiter-gated, and H13 added fail-closed preflight checks for admin-surface readiness and provider-event dispositions before auto-deactivation can proceed.
- Bucket-E payments and multi-currency key preservation has direct source/test coverage through `OrderPaymentStore`, `OrderPaymentStoreTest`, `MultiCurrencyDomainMapTest`, and the repeated restored harness runs recorded in `staging-log.md`.
- Multi-currency has substantial extracted domain coverage, provider-neutral interfaces, compatibility controllers, and Bucket-E key tests, but the B0/B1 baseline is not known-good until the concrete WooPayments imports identified above are removed from the generic domain.
- A3 WooPay/card frontend parity was materially backfilled by H11/H12 with browser evidence, split build assets, appearance/font handling, test-mode checkout copy, and restored harness runs. This does not cover Apple/Google/Amazon express checkout.

### Review Panel

- Hypatia (`architecture-reviewer`): BLOCKED, high 1, persisted in `review-agent-findings.md`.
- Mill (`api-contract-reviewer`): BLOCKED, high 2, persisted in `review-agent-findings.md`.
- Fermat (`reliability-reviewer`): BLOCKED, high 3, persisted in `review-agent-findings.md`.

### N7 Gate-Hardening Status

- Active plan: `plans/2026-06-17-core-native-payments-n7-verification-coverage.md`.
- Financial reconciler status: comparator and script integration are implemented locally. Fixture tests pass for charge amount, capture, refunds, missing provider refund IDs, fee/net, disputes, payouts, same-currency multi-currency meta, and settlement-currency fee/net conversion. Runtime evidence now includes native target partial/full refund orders 152/154, post-refactor native target partial order 156, and reference partial/full refund orders 467/469 reconciling against Stripe raw source. The widened gate exposed and product-fix closed the native refund provider-linkage gap: native refunds now post to the correct WCPay V1 route, and the WooPayments adapter supplies provider-specific refund metadata and note payloads that the generic payment service applies to the matching WC refund.
- Financial reconciler remaining gap: N7(a) is not complete because the full matrix still needs deterministic driven coverage for auth/capture, disputes, payouts, and multi-currency target money paths. Reference dispute order 464 failed correctly because the provider dispute had not reached WC state. Target Test Lab checkout simulation is unsupported in native, so target native money-path driver coverage remains narrower than the spec until native deterministic drivers cover the remaining surfaces.
- Subscriptions gate status: the first-pass N7(b) harness is present and live preflight passed against both reference and target, including WC Subscriptions availability, gateway supports, runtime ownership, and scheduled-renewal/failing-payment-method hook registration. The compare gate remains intentionally blocked until equivalent browser-created reference and target subscriptions exist; do not treat Bucket-C as baseline-green until renewal and failed-renewal runs produce normalized order/meta/email parity evidence.
- Bundle gate status: H18-H20 closed the non-WooPay express checkout missing-asset failures for cart/checkout, pay-for-order, Amazon Pay, and product-page ECE, with Core intentionally split into separate classic/Blocks express assets. H21 closed the concrete classic card CSS product gap by adding and enqueuing Core `woopayments-checkout.css`. H28 closed the source-backed multi-currency settings CSS gap by building/enqueuing Core `multi-currency-admin.css` through the normal WooCommerce admin workflow; final H28 bundle compare passed with `multi-currency-admin.css` measured at `2551` raw / `696` gzip bytes versus reference `22568` raw / `3888` gzip bytes. Do not read this as broad admin/multi-currency green: `multi-currency-analytics.js`, `multi-currency-switcher-block.js`, `multi-currency-setup.css`, and `settings-main.css` remain explicit product-disposition gaps.
- Perf gate status: H17 hardened the first-pass N7(c) perf gate, H21 refreshed it after H20, and H28 refreshed it again with real process/refund/manual-capture fixtures. H28 found and fixed two source-backed regressions before accepting the evidence: failed captures no longer move authorized orders from `on-hold` to `failed`, and native Sift order tracking no longer schedules Action Scheduler jobs unless Sift fraud config is explicitly present. Final H28 money-path perf compare had no structural regression failures: process queries `111 -> 111`, refund queries `21 -> 7`, capture queries `30 -> 34`, gateway count `26 -> 4`, payment-gateway callbacks `6 -> 2`, REST controller instances `28 -> 4`, autoload bytes `115748 -> 85349`, and non-autoloaded `wcpay_account_data`. Local timing medians are noisy smoke signals for large deltas only and are not exact latency proof. N7c remains INCOMPLETE rather than green because isolated REST route-registration timing is still unverified; both stores report `did_action( 'rest_api_init' ) === 1` at WP-CLI eval start and the captures record `route_registration_status=preinitialized`.
- H22 update: the H16 residual non-WooPay Express Checkout Tracks gap for Apple Pay and Google Pay is closed at the contract/test level after H18-H20 introduced native ECE. Native now emits only the reference Apple/Google load/click event names with `{ source }`, honors disabled shopper tracking on classic and Blocks ECE, and leaves Amazon Pay without a button Tracks event because the reference has no Amazon mapping. Deterministic PHP/Jest/build/lint gates passed; Chrome DevTools MCP timed out, so no live wallet browser request capture is claimed.
- H29 update: the refund provider-event subset of the A2/A5 blocker is closed at the deterministic Core level. Native handles `charge.refunded` and `charge.refund.updated` with provider-owned side effects for local refund creation/deletion, `_wcpay_refund_*` metadata, WooPayments notes, split-UPE orders, explicit-currency note formatting, idempotent retry behavior, shared locks, and malformed-payload fail-closed validation. Focused and broader PHP gates passed; no perf or frontend bundle claim is made for this backend webhook slice.
- H30 update: the account/notification provider-event subset of the A2/A5 blocker is closed at the deterministic Core level. Native handles `account.updated`, `account.deleted`, and `wcpay.notification` with provider-owned account refresh/reset side effects, verified account deletion retry markers, preserved payment-method cache cleanup, remote Woo Admin note creation, and malformed/non-durable payload fail-closed behavior. Focused and broader PHP gates passed; no perf, browser, frontend, or timing claim is made for this backend provider-event slice.
- H31 update: the Stripe Billing invoice event subset is closed by retirement, not migration. Native no longer ports `invoice.paid`, `invoice.payment_failed`, or `invoice.upcoming`; instead, cutover fails closed on legacy Stripe Billing markers across subscriptions/orders, the hard marker blocker cannot be removed by the generic preflight filter, and any retired invoice event reaching native logs a webhook alarm before returning deliberately. Bucket C tokenized WC Subscriptions renewal coverage remains intact at deterministic PHPUnit level; end-to-end WC Subscriptions renewal/email parity still belongs to the standing N7b gate.

### N8 Exit Line

N7 is baseline-sufficient as of H31 (`5ac34fdb62`) and should no longer block A4 start. This is not a claim that every residual is fully green; it is an audited triage decision that the widened money-safety, perf/bundle, provider-event, frontend, and multi-currency gates are broad enough to serve as the known-good baseline for A4, with remaining limitations carried into the standing N5/A5 gates.

Resolved or reclassified baseline blockers: H14 non-WooPay express checkout is closed by H18-H20/H22; provider-event disposition is closed at deterministic Core level by H25/H29/H30/H31 and source now shows `KNOWN_UNHANDLED_EVENT_TYPES = array()`; the old B0/B1 multi-currency provider-boundary blocker is resolved by source verification that `plugins/woocommerce/src/Internal/MultiCurrency` no longer imports concrete WooPayments provider classes; webhook exception observability is resolved by `WooPaymentsWebhookRestController::log_webhook_exception()`.

Tracked, non-blocking before A4: payout per-order membership remains a provider/source observability limitation; isolated REST route-registration timing remains unverified because REST is preinitialized in local WP-CLI probes; deterministic target money drivers and WC Subscriptions renewal/email parity stay in the standing N5/A5 evidence checklist; remaining multi-currency analytics/switcher/setup asset dispositions carry into their canonical stage gates.

Cutover-blocking before A5: A4 native merchant admin surfaces remain the largest canonical deliverable and are enforced by `native_admin_surfaces_unavailable`; the two operational queue hooks remain enforced by `operational_queue_hooks_undispositioned`; legacy Stripe Billing markers remain hard-blocked by `legacy_stripe_billing_subscriptions_present`; any future non-empty `KNOWN_UNHANDLED_EVENT_TYPES` list remains cutover-blocking through `provider_events_undispositioned`.

Decision: enter A4 next, and run the N5 stage-boundary spec-conformance gate at A4 exit against this audited baseline.

### A4h / N10 Admin Route Baseline

A4 native admin route architecture is baseline-sufficient as of the A4h exit-gate work. Native WooPayments merchant/admin pages sit under the WC Settings > Payments provider-route seam as `/woopayments/*` sub-routes, and surviving native backend-generated admin links now funnel to `admin.php?page=wc-settings&tab=checkout&path=/woopayments/...` rather than plugin-era `wc-admin&path=/payments/...` URLs. The A4h source gate rejects `/payments/*` route drift in native/settings source and rejects private-registry tokens, so future stage gates can diff against a concrete route/registry assertion instead of an assumed architecture.

Measured native A4 admin chunks in `data/a4-admin-surface-gate-final.json`: settings `10632` raw / `2997` gzip bytes, overview `14490` raw / `3958` gzip bytes, payouts `9215` raw / `2927` gzip bytes, money movement `24688` raw / `6888` gzip bytes, card readers `2512` raw / `1020` gzip bytes, and Capital `7637` raw / `2477` gzip bytes. Aggregate native A4 admin chunks are `69174` raw / `20267` gzip bytes. The same gate measured the reference plugin admin baseline at `1185920` raw / `281792` gzip bytes. The broader bundle gate in `data/n10-a4-bundle-compare.txt` passed with the explicit H28 budget.

Playwriter route-smoke baseline in `data/a4h-browser-probe.json`: provider list, settings, overview, payouts, payout details, transactions, transaction details by `txn_*`, transaction details by `pi_*`, transaction details by `ch_*`, disputes, dispute challenge, card readers, and Capital all matched expected page content; no plugin `woocommerce-payments/dist` scripts loaded; no unexpected failed HTTP responses were recorded. Raw sentinel failures remain intentionally visible: `po_missing` returns a reference-compatible 500 because preserved request-object endpoints wrap provider exceptions as `WP_Error` without REST status data, while `txn_missing`, `pi_missing`, `ch_missing`, and `dp_missing` return endpoint-specific 404s.

The transaction details route contract is now part of the A4 baseline. Native preserves balance-transaction `txn_*` detail support but treats `pi_*` as PaymentIntent details and `ch_*`/`py_*` as charge fallbacks that canonicalize to the PaymentIntent when the provider response includes one. Backend-generated payment and dispute notes keep PaymentIntent/charge IDs as the primary `id` and include `transaction_id=txn_*` as supplemental context when native already has a balance transaction ID. This matches the reference route contract documented in `analysis-a4h-transaction-details-link-contract.md` and prevents recovery links from sending `pi_*` or `ch_*` to `/wc/v3/payments/transactions/{id}`.

The A4h browser gate exposed and fixed a native deposits fatal caused by `WooPaymentsDepositsListRequest::set_param()` narrowing the public parent method to protected. The fixed baseline preserves the legacy `wcpay_list_deposits_request` filter contract and proves third-party callbacks can still call `set_param()` on the request object.

A4h does not make A5 cutover ready by itself. `native_admin_surfaces_unavailable` remains fail-closed until A5 explicitly flips the readiness filter with the standing stage-boundary gate, the operational queue blockers, and the legacy Stripe Billing guard still intact. Documents, Reports, advanced fraud-protection editing, and order-admin adjuncts remain separate/conditional dispositions unless a later canonical gate makes them mandatory.

### A5a Cutover Preflight Baseline Addendum

A5a closes the non-data-dependent cutover preflight blockers that A4h intentionally left fail-closed. Native admin surfaces now default ready only after the A4h/N10 route, registry, browser, and admin-bundle gates; operational queue hooks default to no pending blockers only after native registers and handles `wcpay_instant_deposit_reminder` and `wcpay_post_kyc_activation_email_send`. Provider events remain dispositioned through an empty `WooPaymentsEventIngestor::KNOWN_UNHANDLED_EVENT_TYPES`, while legacy Stripe Billing markers remain hard cutover blockers.

The operational queue baseline now includes both producer and consumer behavior for the preserved hooks. Instant Deposit account refresh and reminder flows preserve the legacy option/note/hook cadence. Post-KYC activation preserves account-refresh KYC completion recording, stage scheduling, email registry resolution, stage consumption only after successful delivery, no-live-sale eligibility, CTA tracking, and the WooCommerce email settings key/template surface. Review findings that initially exposed missing KYC producer coverage and an unsafe email fallback were fixed and re-reviewed with no remaining reliability findings.

The legacy Stripe Billing guard baseline was strengthened during A5a because full target preflight initially timed out after 20 seconds even though provider readiness was true. The guard now checks known legacy marker keys directly in HPOS and CPT storage instead of hydrating orders during preflight, preserving data-safety coverage while keeping the preflight bounded. Target preflight now returns an empty failure list in about 1.05 seconds on the connected native store.

Current A5a evidence: focused A5a PHPUnit passed with 153 tests and 527 assertions, PHPCS and PHPStan passed for touched files, the target runtime hook probe showed native runtime enabled and plugin runtime inactive with all preserved callbacks registered, `bc-drift-gate.sh` passed, `a4-admin-surface-gate.py` passed, and `subscriptions-renewal-gate.sh preflight` passed on reference and target. Post-commit branch lint, changelog validation, and `git diff --check` passed; A5a is committed as `b176692830` plus `f5bf791694` with git range `595a6bfc4a...f5bf791694`. This addendum does not claim full A5 green for browser checkout, money-path, Tracks, refund/dispute/payout, or broad perf gates; those remain standing N5/A5 evidence requirements.

### A5b Cutover Financial Migration Baseline Addendum

A5b closes the cutover readiness line for the preserved WooPayments canceled-authorization fee remediation queue. Native now fails preflight with `financial_migrations_unavailable` when affected canceled-authorization fee data exists and Action Scheduler cannot schedule the live remediation job, and the plugin deactivation path calls `ensure_scheduled()` before `deactivate_plugins()` so there is no deactivated-plugin/no-native-queue window.

The affected-order baseline intentionally matches the reference migration width: canceled intent, bug-start date, refunded orders, and cancelled orders that still have transaction fees. It deliberately does not add an exact payment-method predicate, because legacy data can contain prefixed WooPayments values or missing/mutated method values while still needing remediation. Future tightening of this query needs fresh reference evidence first.

The dry-run baseline is also explicit. If a dry run is enabled or scheduled, native cancels the dry-run action and resets dry-run processing state before checking for live affected orders. This prevents a completed or partially completed dry-run cursor from making cutover think there is no live work. A clean store records `no_affected_orders`; affected stores record `has_affected_orders` and either schedule the live queue or block deactivation loudly.

A5b evidence: focused PHPUnit `WooPaymentsCanceledAuthorizationFeeRemediationServiceTest|WooPaymentsCutoverControllerTest` passed with 52 tests and 144 assertions; syntax, PHPCS changed-file lint, PHPStan for the touched production classes, changelog validation, branch lint, and `git diff --check` all passed. A5b is committed as `c7797478c8` plus `3679904d57` with git range `f5bf791694...3679904d57`. This addendum does not claim fresh browser checkout, full harness, owner-token, local WPCOM connection, refund/dispute/payout, or broad perf coverage. The explorer review reclassified connected-plugin registry membership as unnecessary for this path and carried granular local WPCOM connection readiness, owner user-token readiness, and V1 user-agent/transport continuity into later A5 gate work.

### A5c / N12 Platform Transport and Admin-Parity Reblock Addendum

A5c closes the platform connection and WCPay V1 transport-continuity line for cutover readiness, but N12 supersedes the A5a assumption that the A4h route/registry/bundle gate was enough to default native admin surfaces ready. The current baseline is fail-closed: `WooPaymentsCutoverController::FILTER_NATIVE_ADMIN_SURFACES_READY` defaults false again, and A5 cutover must remain blocked until reopened A4 feature-parity work proves persistent merchant navigation plus functional and visual parity against the reference store.

The platform connection baseline is now explicit. Native cutover preflight requires a connected local WPCOM/Jetpack store, a positive WPCOM blog ID, a connection owner, and an owner user token. These failures are hard platform blockers and are re-added after the broad preflight filter so third-party or local filters cannot hide missing platform prerequisites.

The WCPay transport baseline is also explicit. Native WCPay V1 account updates use owner user-token auth and `User-Agent: WooCommerce Payments/10.8.0`; write requests get an `Idempotency-Key` header while request bodies/GET query params do not leak `idempotency_key`; every attempt includes `X-Request-Initiated`; retry budget is three retries after the initial request and applies only to blocking idempotent transport failures, not local readiness/configuration `WP_Error` paths.

A5c evidence: focused PHPUnit `WooPaymentsApiClientTest|WooPaymentsPlatformConnectionServiceTest|WooPaymentsCutoverControllerTest` passed with 93 tests and 479 assertions; syntax, PHPCS changed-file lint, PHPStan for touched production classes, changelog validation, branch lint, and `git diff --check` all passed. Local A5 readiness probes passed against the target store: `a5-local-wpcom-readiness.sh` passed, `a5-user-token-readiness.php` reported `ready=true` with blog ID 4 and owner user token present, and `a5-transport-continuity.php` captured the expected WCPay V1 account update request with user-token auth and no failures. A5c is committed as `c77fed2d4c` plus `de3773cb89` with git range `3679904d57...de3773cb89`. A5c is not an admin parity green line; reopened A4 feature parity remains the next required slice before admin-surface readiness can be flipped.

### A4i Admin Navigation Reachability Addendum

A4i establishes the first reopened-A4 N12 parity baseline for merchant reachability. Native WooPayments admin surfaces that already have provider routes are now reachable from persistent WP admin navigation under the Core Payments parent menu, and every visible submenu target deep-links into the canonical Settings > Payments provider route namespace. The native implementation does not restore plugin-era `/payments/*` route ownership.

The account-state baseline now includes cached native predicates for rejected accounts, under-review accounts, details-submitted state, valid full-admin-menu account state, card-reader visibility, and Capital-loan visibility. These helpers live in `WooPaymentsAccountService`, so menu projection reuses the preserved defensive cached-account boundary instead of raw option reads.

The runtime ownership baseline is explicit. `WooPaymentsAdminNavigationController::register()` does not add its `admin_menu` callback unless `NativePaymentsRuntimeArbiter::should_native_register()` is true, and the callback rechecks native ownership plus `manage_woocommerce` before mutating the WP admin submenu globals. This preserves the generic Core Payments menu/provider-list orchestration and keeps WooPayments-specific navigation in a provider-owned seam.

A4i evidence: focused PHPUnit passed with 39 tests and 183 assertions for platform helper, admin navigation, account service, and Payments controller coverage; the controller-specific formatting follow-up passed 9 tests and 24 assertions; changed-file PHPCS, production PHPStan, changelog validation, branch lint, `git diff --check`, ignored A4 harness fixture tests, and the widened A4 admin-surface gate all passed. Playwriter proved both direct route rendering and a real WP admin submenu click to Payouts on the target store. A4i is committed through `1400d54ed9` with closeout range `de3773cb89...1400d54ed9`.

This addendum does not complete N12. Reference menu badging, Settings page functional/visual/copy/SCSS parity, dashboard visual/UX parity, Reports/Documents disposition, and the final widened A4 exit gate remain open. `FILTER_NATIVE_ADMIN_SURFACES_READY` must remain fail-closed until those pass.

### A4j Settings Payment Methods and BNPL Addendum

A4j establishes the reopened-A4 baseline for the shared WooPayments settings payment-method row foundation. Native standard payment methods and BNPL no longer render as simplified checkbox/status rows; they now use a shared Core-owned row composition with visually hidden checkbox labels, method icon, visible label/description body, card required marker, card-brand logos, capability status notices/chips, manual-capture incompatibility disabling, and activation-modal gating for unrequested methods with requirements.

The architecture baseline remains clean for this slice. The implementation uses the existing native `wc/payments/settings` store and `/wc/v3/payments/settings` endpoint, does not resurrect `wcpay/data/settings` or `window.wcpaySettings`, and uses Core-owned payment assets through the WooPayments settings chunk. Build output keeps the styling bundled with the native WooPayments settings route rather than global admin or checkout assets.

A4j evidence: focused settings-page Jest passed with 11 tests; focused settings/routes Jest passed with 3 suites and 22 tests; targeted ESLint, targeted Stylelint, admin-library `ts:check`, admin-library bundle build, changelog validation, and `git diff --check` passed after review fixes. Playwriter proved the target native settings route renders the card row with required marker and eight supported card logos including Cartes Bancaires, and the US account row renders `Cash App Afterpay` with the country-specific description. Access logs showed the new Core Cartes Bancaires and Cash App Afterpay SVG assets returning 200, and current-window debug logging added no PHP notices/fatals. The only browser log was Chrome's existing permissions-policy `unload` message.

The A4j review-fix baseline is explicit because the first green line was too narrow. Accessibility review findings were fixed by making the manual-capture banner non-dismissible and adding row-level `Unavailable with manual capture` chips for disabled incompatible methods. Reference-integrity review findings were fixed by adding Core-owned Cartes Bancaires, Afterpay, Cash App Afterpay, and Clearpay SVGs and by mapping Afterpay/Clearpay settings labels/descriptions/icons from `account_country`.

A4j is committed as source/assets/tests/styles `42846f540a` and changelog `47527b39fc`, with git range `1400d54ed9...47527b39fc`. This remains a settings payment-method/BNPL row foundation baseline only; broader A4 parity remains fail-closed.

This addendum does not complete N12 settings parity. Native still lacks or has not yet surfaced the reference extras for payment-method fee pills/tooltips, duplicate-payment-method notices, and PM promotion/discount badges because the native settings payload does not yet expose the needed data shape. Express checkout customize subpages/live preview, payout bank-account block, fraud Basic/Advanced UI, sandbox switch-to-live notice, save busy-state polish, dashboard parity, Reports/Documents disposition, and the widened final A4 exit gate remain open. `FILTER_NATIVE_ADMIN_SURFACES_READY` must remain fail-closed until those pass.

### A4k Settings Row Payload and Extras Addendum

A4k establishes the reopened-A4 baseline for the reference-backed settings row extras that A4j explicitly left out. Native settings now exposes account fee structures, dismissed duplicate-notice state, conservative duplicate gateway clusters, and an explicit empty `pm_promotions` shape because a source-backed native promotions provider does not yet exist. Standard payment-method and BNPL rows now render fee pills/details, discount badges, duplicate-payment notices, and the `book` express button value without resurrecting plugin `wcpay/data/*` stores or global `window.wcpaySettings`.

The row accessibility baseline is now stronger than the first implementation. Fee details are native buttons with expanded/controls/describedby state and Escape close behavior; discount details are associated through hidden description text instead of `title`; duplicate notice dismissal restores focus to the row control; and duplicate notices are suppressed when a row status notice is already present. The gateway duplicate detector fails closed on inspection exceptions and classifies split WooPayments gateway IDs by suffix before generic keyword matching, so `woocommerce_payments_klarna` cannot incorrectly mark the card row.

A4k evidence: focused PHP `WooPaymentsSettingsServiceTest` passed with 17 tests and 184 assertions; focused settings Jest passed with 24 tests; targeted ESLint, targeted Stylelint, admin-library `ts:check`, admin-library bundle build, PHPStan, changed-file PHPCS, changelog validation, branch lint, and `git diff --check` passed. Playwriter proved the target settings route loads the lazy WooPayments settings chunks, displays `$0.30` fee text like the reference store rather than browser-locale `US$0.30`, exposes the `From 2.9% + $0.30` fee disclosure with base/additional/FX/total rows, and closes it with Escape. The target runtime logs had no new PHP notices/fatals during the A4k browser window.

A4k is committed as source/tests/styles `9a4789f3cd` and changelog `624ca6c82d`, with git range `47527b39fc...624ca6c82d`.

A4k does not complete N12 settings parity. Express checkout customize subpages/live preview, payout bank-account block, fraud Basic/Advanced UI and advanced subpage, sandbox switch-to-live notice, save busy-state overlay, source-backed PM promotions, broader copy/content parity, dashboard parity, Reports/Documents disposition, and the widened final A4 exit gate remain open. `FILTER_NATIVE_ADMIN_SURFACES_READY` must remain fail-closed until those pass.

### A4o Admin Shell Badge Parity Addendum

A4o establishes the reopened-A4 baseline for the native WooPayments admin shell badge behavior. Persistent WooPayments submenus under the Core Payments parent now preserve the reference Disputes awaiting-response badge and manual-capture Transactions authorization badge, while continuing to route through Settings > Payments provider subroutes instead of plugin-era top-level WooPayments route ownership.

The count/cache baseline is explicit. Native uses provider-owned cached reads for `wcpay_dispute_status_counts_cache`, `wcpay_test_dispute_status_counts_cache`, `wcpay_authorization_summary_cache`, and `wcpay_test_authorization_summary_cache`; Disputes counts sum only `needs_response` and `warning_needs_response`; authorization counts are skipped unless manual capture is enabled; stale valid data is used on refresh failure; and cold refresh failures store a one-minute errored wrapper to avoid WPCOM refetch loops on every admin page load while still allowing bounded retry.

The provider ownership baseline is also explicit. Native submenu insertion and legacy `/payments/*` redirects are gated on the persisted WooPayments provider `enabled` setting. This intentionally differs from the standalone extension because Core-native WooPayments-specific persistent navigation should not claim the Core Payments provider space while that provider is disabled. When enabled, legacy deep links still migrate to the canonical native Settings > Payments route. Reports and Documents remain intentionally absent until their UI/API surfaces are ported in separate A4 slices.

A4o evidence: focused PHPUnit passed with 111 tests and 642 assertions; focused route Jest passed with 1 test; the widened A4 admin surface gate passed and recorded chunk sizes; changed-file PHP lint, production PHPStan, targeted ESLint, ignored gate Python syntax, changelog validation, final branch lint, and `git diff --check` passed. Playwriter proved seeded `Transactions 5` and `Disputes 3` badges plus Disputes `filter=awaiting_response`, and separately proved disabled-provider legacy URLs do not redirect while enabled-provider legacy URLs do. The cross-store harness passed 7/7 with reference order 569 and target order 268, including widened money reconciliation for the captured charge path. Runtime log scans found no PHP notices/warnings/fatals or HTTP 500s in the target, reference, or local WPCOM containers for the verification windows.

A4o does not complete N12. Overview/dashboard parity, Payouts/Transactions/Disputes list/detail parity, Card Readers/Capital framing/styling, Reports/Documents native UI/API slices, source-backed PM promotions, broader copy/content parity, and the widened final A4 exit gate remain open. `FILTER_NATIVE_ADMIN_SURFACES_READY` must remain fail-closed until those pass.

### A4p PM Promotions and Spotlight Baseline Checkpoint

A4p is now closed as a reopened-A4 slice. The native implementation restores the reference PM promotions contract at the right provider level: `WooPaymentsApiClient` owns the `payment_method_promotions` transport and preserved request hooks through concrete legacy-compatible request classes, `WooPaymentsPmPromotionsService` owns filtering/cache/dismissals/activation/settings-save activation, the existing native WooPayments REST controller exposes the preserved `/wc/v3/payments/pm-promotions` route family, and the frontend uses a dedicated `wc/payments/pmPromotions` store instead of resurrecting plugin-global `wcpay/data/*` state.

The parity checkpoint includes the reference invariants verified against source and tests: required promotion fields match the standalone WooPayments service, enabled/dismissed/invalid/discounted/later-promo-id filtering is preserved, promotion availability follows settings/account payment-method availability rather than global capability IDs, activation and dismissal produce success/error notices, discount badges suppress promotion row badges, and spotlight events classify encoded native Settings > Payments provider sub-routes without leaking full paths or query strings into Tracks payloads.

A4p evidence: focused PHPUnit passed with 175 tests and 976 assertions; focused admin Jest passed with 6 suites and 61 tests; PHP syntax, changed-file PHPCS, PHPStan, targeted ESLint, targeted Stylelint, admin-library `ts:check`, admin-library `build:project`, changelog validation, branch JS lint, full branch lint, and `git diff --check -- . ':!.agents'` passed. The admin build confirms `client/woopayments/promotions/style.scss` is handled by the normal WooCommerce admin bundle workflow and scoped to WooPayments chunks. The widened ignored A4 admin-surface gate now asserts PM promotion source contracts and passed, writing `data/a4p-admin-surface-gate-final.json`. Playwriter target proof with deterministic seeded promotions confirmed spotlight rendering, row badge rendering, dialog semantics, Escape close behavior, and focus restoration; the target seed was restored afterward and the post-window Docker log scan found no PHP diagnostics.

This checkpoint completes A4p only; it does not complete N12. Overview/dashboard parity, Payouts/Transactions/Disputes list/detail parity, Card Readers/Capital framing/styling, Reports/Documents native UI/API slices, broader copy/content parity, and the widened final A4 exit gate remain open. Native admin readiness remains fail-closed, and A4 remains scheduled to reopen after A5c per the task list before A5 readiness can be reconsidered.

### A4aa Settings Contact Validation Baseline Addendum

A4aa closes the reopened-A4 settings contact/notification/advanced parity cluster. Native WooPayments settings under the Core Settings > Payments provider route now preserve the reference owner-email warning, email validation, changed-email confirmation, customer-support copy and validation, dev-mode debug behavior, and deprecated bundled-subscriptions fail-closed enablement, while keeping Stripe Billing retired and keeping native admin readiness fail-closed.

The shared component baseline changed deliberately: `@woocommerce/components` now exports `validatePhoneNumber`, and `PhoneNumberInput` forwards scoped ARIA/onBlur props so provider settings can use the same phone parsing/validation path without duplicating phone logic in the WooPayments settings chunk. This keeps card rendering and WooPay abstractions separate; the phone input support is a shared components API used by settings validation.

A4aa evidence: focused admin Jest passed with 2 suites and 43 tests, focused components Jest passed with 2 suites and 13 tests, admin `ts:check`, components type lint, targeted ESLint/Stylelint, components/admin bundle builds, WooCommerce/components changelog validation, `git diff --check -- . ':!.agents'`, and branch lint all passed. Playwriter proved invalid support phone/email and notification confirmation mismatch behavior on the target settings route with no failed responses or console messages, target `debug.log` stayed empty, final a11y and reference/API reviews approved, and the A4 admin surface gate passed with evidence in `data/a4aa-admin-surface-gate.json`. A4aa is committed as `99453ab9fc` plus changelog `44c9640bac`, with git range `9c735d2cb0...44c9640bac`.

A4aa does not complete N12. The final A4 exit gate still needs full route/account-state matrix, copy/content, disabled-feature no-chunk/no-REST behavior, visual/functional parity, log scans, and bundle evidence across accumulated A4 before `FILTER_NATIVE_ADMIN_SURFACES_READY` can be reconsidered.

### A4ab Widened Admin Exit Gate Baseline Addendum

A4ab establishes the first accumulated-A4 browser/runtime gate baseline after the reopened parity slices A4i through A4aa. It keeps WooPayments native admin surfaces under the Core Settings > Payments provider route seam and proves the accumulated local target/reference route matrix loads in a real browser without target failed responses, target console issues, or target PHP/WP debug-log diagnostics. It does not flip native admin readiness; `FILTER_NATIVE_ADMIN_SURFACES_READY` remains fail-closed for a later A5 readiness/stage-boundary slice.

The A4ab product baseline includes two source-backed fixes surfaced by the gate. Native Overview now has a stable route-level `Overview` heading while embedded WooPayments settings render as a subsection heading, preserving standalone account settings as `h1`. Multi-currency analytics registration no longer resolves the default store currency during WooCommerce construction; selected-currency order select projections still apply at clause runtime for non-default requests and no-op for default-currency requests.

The A4ab harness baseline is stricter than the first browser run. The ignored Playwriter gate now records script and resource timing evidence, fails target routes missing expected lazy native chunks, and only waives `useSelect` console warnings for the reference store. The final browser evidence in `data/a4ab-admin-browser-gate.json` has 57 checks and 0 failures; every lazy target route has its expected `settings-payments-woopayments-*` chunk in resource timing, while the top-level WC settings route is explicitly marked as a local server-rendered route with no lazy native chunk observed.

A4ab evidence: focused frontend Jest passed with 24 Overview tests and 10 account-settings tests; focused PHP `MultiCurrencyAnalyticsControllerTest` passed with 11 tests and 29 assertions; adjacent multi-currency PHP coverage passed with 76 tests and 213 assertions; syntax, changed-file PHPCS, PHPStan, admin `ts:check`, targeted ESLint/Stylelint, admin bundle build, changelog validation, branch lint, and `git diff --check` passed. The source/chunk gate wrote `data/a4ab-admin-surface-gate.json` with total native admin chunks `197638` raw / `53642` gzip. Target `debug.log` stayed at 0 lines after the full matrix and focused heading proof; reference logs still contain reference-side settings/plugin noise. A4ab is committed as `410ad0e3ec` plus changelog `c293d2f1cc`, with git range `44c9640bac...c293d2f1cc`.

A4ab does not claim full pixel/copy parity, exhaustive account-state coverage, or disabled-feature no-REST/no-chunk coverage beyond what the local route matrix can prove. Future N12/A5 gates should diff against this audited smoke/runtime/chunk/log baseline and fail closed on new target route, asset, console, or debug-log regressions.

### A5d Native Admin Readiness Addendum

A5d consumes the A4ab baseline as the source-backed evidence for flipping only the native admin-surface readiness default. `WooPaymentsCutoverController::FILTER_NATIVE_ADMIN_SURFACES_READY` now defaults true, so `native_admin_surfaces_unavailable` no longer blocks otherwise-clean native preflight after the A4 route/chunk/browser/log gate. The explicit false-filter override remains a hard blocker and is covered by `WooPaymentsCutoverControllerTest::test_preflight_blocks_when_native_admin_surfaces_are_unavailable()`.

This addendum does not alter the wider cutover safety posture. Platform connection and owner user-token readiness, provider-event disposition, operational-queue readiness, financial remediation, legacy Stripe Billing subscription guard, plugin-active/capability checks, and mandatory cutover default-off behavior remain independent hard gates. Reliability review source-checked those blockers at the controller boundary and approved with no findings; WordPress architecture review approved the provider-level cutover seam with no findings.

A5d evidence: RED `WooPaymentsCutoverControllerTest::test_preflight_defaults_to_ready_after_native_admin_surfaces_are_verified` failed before the default flip; GREEN `WooPaymentsCutoverControllerTest` passed with 32 tests and 78 assertions; the related readiness suite passed with 158 tests and 542 assertions; syntax, changed-file PHPCS, PHPStan, changelog validation, branch lint, and `git diff --check -- . ':!.agents'` passed. The source/chunk gate wrote `data/a5d-admin-surface-gate.json` with total native admin chunks `197685` raw / `53668` gzip and reference plugin admin baseline `905253` raw / `217657` gzip. The strict Playwriter matrix wrote `data/a5d-admin-browser-gate.json` with 57 checks and 0 failures. A5 local WPCOM readiness, user-token readiness, transport continuity, and target native preflight probes passed, with preflight failures empty on the target store.

A5d log evidence: target `wp-content/debug.log` was cleared before the final probe rerun and stayed at 0 bytes; focused target/local-WPCOM Docker scans found no PHP fatal, parse, warning, notice, deprecated, uncaught exception, critical error, or database-error signatures. `soft_notice` was false in the target preflight probe because the standalone WooPayments plugin is not active on that store, not because preflight failed.

A5d is committed as `e7612bf9e7` plus changelog `2555f71968`, with git range `c293d2f1cc...2555f71968`. It is not a new pixel/copy/account-state parity claim; future stage gates must diff against the A4ab/A5d smoke/runtime/chunk/log baseline and fail closed on new regressions.

### A5e Cutover Handoff Addendum

A5e establishes the local browser/runtime baseline for the plugin-to-native handoff itself. The soft cutover flow is now proven from a plugin-owned target state through the product-generated disable action to native ownership. The mandatory cutover path is also proven locally with a temporary helper that enables the rollout filter only for the proof window; mandatory cutover remains default-off in product code.

The product baseline now distinguishes notice status source. A trusted current-request status produced by `maybe_auto_deactivate_plugin()` can render success even during the WordPress overlap where the plugin active option has been removed but the `WC_Payments` class remains loaded in the current PHP process. Query-string or transient `disabled` statuses are not trusted the same way: if the plugin runtime is still active, native renders the blocked notice instead. Failed deactivation clears stored success and renders blocked, and same-request success clears the stored status to avoid replay.

A5e evidence: the soft Playwriter gate wrote `data/a5e-soft-cutover-browser-gate.json` with 0 failed responses and 0 console issues; mandatory Playwriter wrote `data/a5e-mandatory-auto-deactivation-browser-check.json` with 3 checks, 0 failed responses, and 0 console issues; final native state is recorded in `data/a5e-state-after-mandatory-rerun-final.json`. Focused `WooPaymentsCutoverControllerTest` passed with 37 tests and 91 assertions; the related readiness suite passed with 170 tests and 573 assertions; syntax, changed-file PHPCS, production PHPStan, changelog validation, branch lint, and `git diff --check -- . ':!.agents'` passed. Reliability and WordPress architecture re-reviews approved with no findings after the loaded-class overlap fix and the explicit stale-transient guard test.

A5e log evidence: the target debug log stayed at 0 bytes after the final mandatory browser proof, and target/local-WPCOM scans found no PHP notices, warnings, deprecations, fatals, uncaught exceptions, database errors, or actual HTTP 5xx lines in the proof window.

A5e is committed as `5a2a042da5` plus changelog `7896e66ac7`, with git range `2555f71968...7896e66ac7`.

A5e does not widen money-path, checkout, visual parity, or mandatory-rollout claims. It should be treated as the cutover handoff baseline that later A5/A6 gates diff against when mandatory rollout, plugin sunset, or additional handoff states are exercised.

### A4ac Admin Readiness Reground Addendum

A4ac corrects the A5d native-admin readiness default after re-reading N8 and N12. The source-backed decision is that A4ab remains a valuable smoke/runtime route/chunk/log baseline, but it does not satisfy N12's full merchant-facing parity bar for account-state breadth, disabled-feature no-REST/no-chunk behavior, visual/copy parity, or full functional parity. Therefore `WooPaymentsCutoverController::FILTER_NATIVE_ADMIN_SURFACES_READY` must default fail-closed again until a later explicit N12 parity gate authorizes the flip.

The product baseline now has `WooPaymentsCutoverController::get_preflight_failures()` call `apply_filters( FILTER_NATIVE_ADMIN_SURFACES_READY, false )`. The explicit `true` filter remains available for controlled tests and later gate proofs, but production/default preflight once again reports `native_admin_surfaces_unavailable` unless a readiness gate deliberately marks admin surfaces ready.

A4ac evidence: RED `WooPaymentsCutoverControllerTest::test_preflight_defaults_admin_surfaces_to_unavailable_until_n12_parity_gate_passes` failed against the A5d default-ready code because `native_admin_surfaces_unavailable` was absent; GREEN passed after restoring the fail-closed default. Focused `WooPaymentsCutoverControllerTest` passed with 37 tests and 91 assertions; the related readiness suite passed with 171 tests and 575 assertions; PHP syntax, changed-file PHPCS, and production PHPStan passed. Local target preflight evidence in `data/a4ac-admin-readiness-preflight.json` shows native ownership with `preflight_failures` containing only `native_admin_surfaces_unavailable`, `ready=false`, and `soft_notice=false`. Target `debug.log` stayed empty, and corrected target/local-WPCOM log scans found no PHP diagnostics or HTTP 5xx for the proof window.

A4ac is committed as `4d1d643d05` plus changelog `39cd54073d`, with git range `7896e66ac7...39cd54073d`.

A4ac is a corrective reground slice, not a new feature parity slice. A4/N12 remains open, A5e handoff proof remains valid, and future A4 work should continue from source-backed parity residuals rather than treating A4ab/A5d smoke/runtime evidence as full merchant-facing parity authorization.

### A4ad Account-State Admin Availability Addendum

A4ad strengthens the reopened-A4/N12 baseline by aligning direct native WooPayments provider routes with the same account-state and optional-feature availability that persistent admin navigation already used. The baseline contract is now that unavailable protected WooPayments admin routes render an accessible unavailable status before importing their lazy chunk or calling their protected admin REST surface. Provider settings, express checkout settings, and fraud settings remain reachable through the Core Settings > Payments provider route seam because merchants must still be able to manage the provider.

The backend availability baseline is centralized in `WooPaymentsAdminNavigationController::get_admin_route_availability()` and is derived from existing `WooPaymentsAccountService` predicates rather than raw account option reads. The route projection records gateway enabled state, a coarse account state, and explicit route booleans for settings, onboarding, overview, payouts, transactions, disputes, Reports, Card Readers, Capital, and Documents. Legacy `/payments/*` redirect handling consumes that same projection, so unavailable plugin-era deep links do not silently migrate into native protected surfaces.

The Capital baseline was corrected after review. Native Capital REST routes and legacy `admin.php?wcpay-loan-offer` redirects now require gateway enabled, valid admin-navigation account, not rejected, not under review, and previous Capital-loan eligibility. This keeps Capital route registration aligned with the native menu and direct route projection instead of only checking loan history.

A4ad evidence: focused PHP passed with 112 tests and 484 assertions; focused route Jest passed with 22 tests; targeted ESLint, PHP syntax, production PHPStan, changed-file PHPCS, admin `ts:check`, admin bundle build, changelog validation, `git diff --check -- . ':!.agents'`, and branch `lint:changes:branch` passed. The full A4ad Playwriter matrix recorded 57 checks and 0 failures in `data/a4ad-admin-browser-gate.json`; the post-review target Capital rerun recorded 3 checks and 0 failures in `data/a4ad-capital-target-postfix-admin-browser-gate.json`. The ignored browser harness now fails closed on unknown filters, requires selected work, streams progress, and records browser `pageerror` events.

A4ad does not complete N12. It closes the availability/no-chunk route contract for currently registered native admin surfaces, but full visual/copy/detail parity, payment-detail/dispute-action residuals, WooPay/settings residuals, and the final widened account-state/browser/log/bundle gate remain open. Native admin readiness remains fail-closed until that later gate explicitly authorizes a flip.

### A4ae Payment Detail and Dispute Action Baseline Addendum

A4ae closes the source-backed payment-detail/dispute-action parity gap where native transaction details was only a compact detail/timeline page while the reference payment details route acted as the dispute decision hub. Native actionable dispute rows now route through transaction details, and transaction details renders the dispute decision context before the evidence challenge flow.

The native baseline now preserves dispute data from charge and PaymentIntent responses, including charge-level dispute precedence over intent-level dispute data. The `Dispute details` section shows dispute ID, reason, response-needed/resolved status labels, response due date, amount, optional customer context, challenge/submit-evidence route, warning-inquiry refund guidance, accept-dispute confirmation, status-specific resolved/under-review copy, and submitted-evidence links. The accept path uses the existing native close endpoint and does not introduce a new money-moving route.

The reliability baseline now invalidates dispute caches after successful close only. `wcpay_dispute_status_counts_cache`, `wcpay_test_dispute_status_counts_cache`, and `wcpay_active_dispute_cache` are deleted after the platform accepts the close, while platform failures keep stale caches intact and surface the error. This makes accepted disputes disappear from counts/lists without concealing failed close attempts.

The accessibility baseline covers the async decision paths. The action row accessible name starts with visible text (`Respond now`) and identifies the transaction-detail destination. The accept modal keeps disabled busy actions accessible, warning inquiry refund guidance is described while disabled, successful accept focuses the persistent `Dispute details` heading, manually moved outside focus is not stolen, and focus lost to `body` or still inside the dispute response area is restored to the heading after the action UI is replaced.

A4ae evidence: focused admin Jest passed with 29 tests; focused PHP passed with 34 tests and 219 assertions; targeted ESLint, admin type lint, admin style lint, PHP syntax, changed-file PHPCS, PHPStan, admin bundle build, changelog validation, `git diff --check -- . ':!.agents'`, and branch lint passed. Playwriter verified the target dispute list to transaction-detail route, the detail decision section, and non-mutating accept modal cancel path with clean browser console and clean target PHP log scans. General code, reliability, and a11y reviews approved after source-backed fixes. The slice is committed locally as `9232b9d5eb` plus changelog `0ca1d25347`, with git range `0c3f94350c...0ca1d25347`.

A4ae does not complete N12. Refund modal parity, detail-level capture/cancel/fraud-review actions, broader visual/copy/payment-detail parity comparison, WooPay/settings residual parity, and the final accumulated A4 exit gate remain open. Native admin readiness remains fail-closed.

### A4af WooPay Disable Feedback Baseline Addendum

A4af closes the source-backed WooPay settings residual where native settings could disable WooPay but did not preserve the reference last-disable-date side effect or open the merchant feedback modal after a successful disable save. This is a settings/admin parity baseline only; it does not change checkout behavior, money movement, cutover readiness, or the standalone WooPayments plugin.

The backend baseline now returns `woopay_last_disable_date` in native settings and records `platform_checkout_last_disable_date` only when a request explicitly changes WooPay from enabled to disabled. Existing dates are preserved when WooPay was already off, and updates that omit WooPay do not create a false disable event. The shared settings data store merges POST response fields over submitted settings so server-derived values stay current after a same-session save/remount.

The frontend baseline now mirrors the reference trigger: remember the first loaded WooPay enabled state, wait for a truthy `saveSettings()` result, require current WooPay disabled state, and apply a seven-day throttle from `woopay_last_disable_date`. The modal is Core-owned and scoped to the native settings route, uses WordPress `Modal`/`Spinner`, the existing WooPay logo SVG, a polite loading status, and the reference survey iframe URL.

A4af evidence: focused PHP passed with 23 tests and 231 assertions; focused admin Jest passed with 54 tests; targeted ESLint, admin type lint, admin style lint, PHP syntax, changed-file PHPCS, production PHPStan, admin bundle build, changelog validation, `git diff --check -- . ':!.agents'`, and branch lint passed. Playwriter verified the target settings route opens the `WooPay feedback` modal and survey iframe after disabling WooPay; evidence is in `data/a4af-woopay-disable-feedback-browser.json` and `data/a4af-woopay-disable-feedback-modal.png`. The target option was restored afterward, target `debug.log` stayed empty, and target logs had no PHP/WP diagnostics for the proof window. A11y and API-contract reviews approved with no critical/high/medium findings after residual fixes.

A4af is committed locally as `7faed3e4fe` plus changelog `6870a9caec`, with git range `0ca1d25347...6870a9caec`.

A4af does not complete N12. Native money-detail refund/capture/fraud-review actions, manual-capture confirmation and save-error focus, express overview copy/legal/Amazon availability/duplicate-detection/live-preview gaps, broader visual/copy comparison, and the final accumulated A4 exit gate remain open. Native admin readiness remains fail-closed.

### A4ag Settings Merchant Parity Baseline Addendum

A4ag closes the next source-backed native settings merchant-parity cluster. Native settings now preserves the reference manual-capture enable contract by opening a confirmation modal before enabling, while keeping disable immediate. Failed settings saves now keep the generic save failure status, suppress the duplicate raw `server_error` notice when server field details exist, and move focus to the first known field-level error so merchants land on the actionable control. Express checkout overview rows now render the WooPay, Apple Pay / Google Pay, Link, and Amazon Pay legal/read-more links rather than flattening that reference copy into plain strings.

The native duplicate-detection baseline now includes the reference synthetic Apple Pay / Google Pay duplicate cluster. `WooPaymentsSettingsService` recognizes `apple_pay_google_pay` through a provider-level declaration filter and through narrow payment-request heuristics for native WooPayments, Stripe payment-request gateways, and Apple/Google keyword gateways. The previous broad generic `express_checkout_enabled` option fallback was deliberately not kept because it could misclassify unrelated gateways and couples generic option names to WooPayments-specific duplicate semantics.

The frontend baseline reuses existing settings-row availability and duplicate-notice contracts instead of creating a second express-only path. Amazon Pay overview notices now follow method status, Apple Pay / Google Pay duplicate notices use the same dismiss/focus persistence behavior as payment-method rows, and express checkbox ids are deterministic enough for focus restoration and tests.

A4ag evidence: focused PHP passed with 28 tests and 238 assertions; focused admin settings Jest passed with 64 tests across the settings page and settings data store; targeted ESLint, admin type lint, PHP syntax, changed-file PHPCS, production PHPStan, admin bundle build, changelog validation, branch JS/PHP lint, and `git diff --check -- . ':!.agents'` passed. Playwriter proved the target settings route renders the express legal/read-more links, opens the manual-capture confirmation modal, cancels without saving, keeps manual capture off, reports no failed browser responses, and leaves target `wp-content/debug.log` at 0 bytes after clearing. A4ag is committed locally as `377a6cbe76` plus changelog `30cdbcf46f`, with git range `6870a9caec...30cdbcf46f`.

A4ag does not complete N12. Live Stripe Elements preview, refund modal parity, detail-level capture/cancel/fraud-review actions, broader visual/copy/account-state comparison, and the final accumulated A4 exit gate remain open. Native admin readiness remains fail-closed.

### A4ah Detail Authorization Actions Baseline Addendum

A4ah closes the detail-level authorization action gap that A4ag left open. Native transaction details now uses the same existing order-scoped authorization endpoints as the uncaptured list: ordinary uncaptured PaymentIntent details show the seven-day capture notice with `Capture`, while fraud-review PaymentIntent details show `Block transaction` and `Approve transaction`. Normal detail-level cancel remains hidden because the reference exposes cancel in this context as the fraud-review block action, not as a generic non-fraud detail cancel.

The money-safety baseline remains backend-owned. The frontend action handler only calls `captureWooPaymentsAuthorization( orderId, paymentIntentId )` or `cancelWooPaymentsAuthorization( orderId, paymentIntentId )`; `WooPaymentsAuthorizationsRestController` continues to enforce order existence, order-intent ownership, refunded-order rejection, live PaymentIntent ownership/status, fraud outcome metadata, and expected action outcome. A4ah adds no new refund or authorization mutation route.

The frontend reliability baseline now covers stale route and async focus behavior. Successful action reloads are route-key guarded so an old detail request cannot overwrite a newer route, while action failures still dispatch an error notice even if the merchant navigated away. After success, focus moves to the persistent `Payment details` heading only when focus was lost or remains inside the replaced authorization action/notice region; user-moved focus elsewhere is preserved.

A4ah evidence: focused admin Jest passed with 38 tests; focused PHP `WooPaymentsMoneyMovementRestControllerTest` passed with 34 tests and 219 assertions; exact-file ESLint, admin type lint, targeted Stylelint, admin bundle build, changelog validation, branch lint, and `git diff --check -- . ':!.agents'` passed. Playwriter session `77` verified the target native transactions list, uncaptured list, and a current PaymentIntent detail route with no failed responses, no page errors, and only the known Chrome `unload` permissions-policy console message; evidence is in `data/a4ah-transaction-detail-browser.json` and `data/a4ah-transaction-detail-browser.png`. Target `debug.log` stayed at 0 bytes and target/local WCPay simulator log scans found no PHP/WP diagnostics for the proof window. Reliability, accessibility, and JS test reviews are recorded in `review-agent-findings.md`. A4ah is committed locally as `7c045db09d` plus changelog `f382f3a290`, with git range `30cdbcf46f...f382f3a290`.

A4ah does not complete N12. Refund modal parity, the live Stripe Elements settings preview, broader payment-detail visual/copy comparison, account-state matrix breadth, and the final accumulated A4 exit gate remain open. Native admin readiness remains fail-closed.

### A4ai Refund Modal Parity Baseline Addendum

A4ai closes the order-backed transaction-detail refund action gap left open by A4ah. Native transaction details now shows `Transaction actions` for captured, order-backed, not-fully-refunded transactions, opens a `Refund transaction` modal with optional reason selection, preserves open-inquiry warning copy, and sends full refunds through `/wc/v3/payments/refund`. Partial refunds route merchants back to the WooCommerce order edit page instead of adding a second partial-refund UI inside the payment-detail surface.

The money-safety baseline remains order-backed. The native refund route validates the local order, verifies the request charge matches the order `_charge_id`, interprets Stripe minor-unit amounts against the order currency, rejects over-refunds before mutation, and calls `wc_create_refund( refund_payment => true, restock_items => true )` so gateway refund transport, order notes, refund records, metadata, and follow-up order/webhook behavior stay inside the existing WooCommerce money path. Direct no-order charge refunds remain intentionally unimplemented and must be handled by a separate money-safety design if needed.

The detail-order enrichment baseline is corrected. Native charge and PaymentIntent detail responses are enriched from local WooCommerce order context through `WooPaymentsMoneyMovementOrderService`, including `intent.charge` and `intent.charges.data[]`, so detail actions do not depend on list-only order data. The frontend still falls back to parsing the order URL when `order.id` is absent, matching HPOS/legacy admin URL shapes used by the local runtime.

A4ai evidence: focused PHP `WooPaymentsMoneyMovementRestControllerTest` passed with 40 tests and 254 assertions; focused admin Jest `money-movement-pages.test.tsx` passed with 44 tests; exact-file ESLint, admin type lint, targeted Stylelint, PHP syntax, changed-file PHPCS, production PHPStan, admin bundle build, changelog validation, `git diff --check -- . ':!.agents'`, and branch lint passed. Playwriter session `78` verified the target transaction-detail actions menu, modal copy, reason radios, partial-refund order link, and non-mutating cancel path with no failed browser responses or page errors. Target `debug.log` stayed 0 bytes and target PHP/log scans found no request-path diagnostics; WPCOM jobs logs still contain unrelated background wpcom-local PHP 8.4 deprecation/database noise.

A4ai is committed locally as source/tests `19b03bb680` plus changelog `761690b187`, with git range `f382f3a290...761690b187`.

A4ai does not complete N12. Live Stripe Elements settings preview, broader payment-detail visual/copy comparison, account-state matrix breadth, and the final accumulated A4 exit gate remain open. Native admin readiness remains fail-closed.

### A4aj Express Checkout Preview Baseline Addendum

A4aj closes the source-backed express checkout settings preview gap. Native Apple Pay / Google Pay settings no longer render only the static requirements fallback; the settings route now has a Core-owned, route-scoped preview that can show the disabled-express notice, a local WooPay preview, or Stripe's Express Checkout Element when HTTPS, public Stripe config, and wallet availability allow it.

The settings contract now exposes public preview config under `express_checkout_preview.stripe`. The payload is sourced from `WooPaymentsAccountService` for publishable key and connected account ID plus a Stripe-compatible locale. This keeps preview configuration at the native WooPayments provider settings layer, avoids Stripe Billing coupling, and does not expose secret account material beyond what shopper-facing express checkout already receives.

The frontend baseline keeps WooPayments-specific preview logic bundled with the WooPayments settings sub-route. It uses `window.Stripe` and Stripe.js lazy injection rather than adding global admin Stripe React dependencies, mounts Elements with `loader: 'never'`, maps the existing button type/size/theme/radius controls into the preview, forces Apple Pay / Google Pay for the preview, disables other wallets there, and fails closed to existing notices when the page is HTTP, config is incomplete, Stripe.js fails, or no wallets are available. Failed Stripe.js loads now retry from a fresh script node instead of reusing stale failed DOM.

The accessibility baseline now preserves the visible WooPay CTA as the button accessible name and adds `Express checkout preview` as description text. The preview button remains removed from tab order and marked disabled for interaction purposes, while notices retain the WordPress notice/announcement path.

A4aj evidence: focused PHP `WooPaymentsSettingsServiceTest` passed with 28 tests and 240 assertions; focused admin Jest `express-checkout-settings.test.tsx` passed with 22 tests; exact-file ESLint, admin type lint, targeted Stylelint, PHP syntax, changed-file PHPCS, production PHPStan, admin bundle build, changelog validation, `git diff --check -- . ':!.agents'`, and branch lint passed. Playwriter session `79` passed the filtered target desktop/mobile settings express browser gate and focused preview capture, with no failed browser responses, empty harness console issues, and target `debug.log` empty after clearing. Architecture, reliability, and accessibility reviews were source-backed and fixed before commit.

A4aj is committed locally as source/tests `240b80c7dd` plus changelog `becafbb2cd`, with git range `761690b187...becafbb2cd`.

A4aj does not complete N12. Live HTTPS wallet rendering remains source/Jest-covered rather than deterministic browser-proven in the local HTTP target. Broader payment-detail visual/copy comparison, account-state/browser/log/bundle breadth, checkout/card visual parity, and the final accumulated A4 exit gate remain open. Native admin readiness remains fail-closed.

### A4ak Payment Detail Summary Composition Baseline Addendum

A4ak closes the read-only payment-detail summary-composition gap left after A4aj. Native payment details now render the reference-level summary grouping, current-account test-mode notice, self-describing order/subscription/customer links, missing-order guidance, identifiers, card/card-present/interac-present payment-method details, and a generic non-card fallback without changing the existing money-moving refund/capture/dispute flows.

The data baseline now preserves richer detail context from the native money-movement responses: order, customer, subscription, payment-method, address, check, metadata, identifiers, channel, and status fields are normalized before rendering. Sales channel derives from payment-method/channel metadata rather than transaction row type, and the test-mode notice is explicitly scoped to the current WooPayments account state instead of claiming historical payment-created mode.

A4ak evidence: focused admin Jest passed with 47 tests; exact-file ESLint, admin type lint, targeted Stylelint, admin bundle build, widened A4 source/chunk gate, Playwriter desktop/mobile proof, clean target/reference debug logs and strict target/local-WPCOM log scans, changelog validation, `git diff --check -- . ':!.agents'`, staged diff checks, and branch lint passed. The slice is committed locally as `d567290579` plus changelog `84791320b2`, with git range `becafbb2cd...84791320b2`.

A4ak does not complete N12. `card_reader_fee` routing/table parity, richer timeline parity, broader payment-method variants, checkout/card visual parity, account-state/browser/log/bundle breadth, and the final accumulated A4 exit gate remained open at this checkpoint. Native admin readiness remains fail-closed.

### A4al Payment Detail Residual Baseline Addendum

A4al closes the next payment-detail residual cluster: native card-reader-fee detail routing now prefers provider `metadata.charge_type`, renders a dedicated Card readers summary table and local CSV export branch, suppresses the generic parent loaded announcement on reader-fee routes, and adds loading, empty, timeout, and error states for the reader-fee summary path.

The timeline and payment-method baseline are widened without becoming unbounded. Native details now render a bounded timeline over the existing provider timeline payload and display non-card payment-method rows only for fields currently present in the native payload, while card/card-present/interac-present continue to use richer dedicated sections. Reader-fee fetches forward `AbortSignal` and abort after 15 seconds.

A4al evidence: focused admin Jest passed with 69 tests; exact-file ESLint, admin type lint, admin bundle build, widened A4 admin surface gate, real normal-charge Playwriter proof, synthetic reader-fee branch proof, clean target/reference debug logs and Docker scans, changelog validation, `git diff --check -- . ':!.agents'`, and branch lint passed. The slice is committed locally as `995c3e332e`, with git range `84791320b2...995c3e332e`.

A4al does not complete N12. Checkout/card visual parity, Reports reader-fee aggregation, account-state/browser/log/bundle breadth, and the final accumulated A4 exit gate remained open at this checkpoint. Native admin readiness remains fail-closed.

### A4am Classic Card Checkout Baseline Addendum

A4am closes the source-backed classic card checkout visual/runtime drift. Native classic card checkout now passes card-owned Stripe Elements options including `loader: 'never'`, DOM-derived appearance, allowed font CSS rules, manual Payment Element options, and setup-mode amount omission parity independent of WooPay. Classic card brand rendering now uses provider config brand icons, displays the visible logo set plus `+ N`, and opens an accessible supported-brand dialog for hidden brands.

The frontend asset baseline keeps this WooPayments-specific classic card code in the classic WooPayments checkout bundle. Styling for the logo row, overflow dialog, and card-label float correction lives in the WooPayments checkout CSS rather than generic WooCommerce checkout assets, preserving feature-owned performance and avoiding coupling card rendering to WooPay or Blocks express checkout.

A4am evidence: focused legacy Jest passed with 15 tests; focused PHP `NativeWooPaymentsGatewayTest` and `WooPaymentsCheckoutBridgeTest` passed; exact-file ESLint, targeted Stylelint, PHP syntax via unit/bootstrap, changed-file PHPCS, production PHPStan, classic asset build, changelog validation, `git diff --check -- . ':!.agents'`, and branch lint passed. Playwriter proved the target classic checkout card row, test-card copy, visible logos, overflow dialog, keyboard semantics, and focus return. The slice is committed locally as source/tests `fb2a0b2e88` plus changelog `d101c1d251`, with git range `995c3e332e...d101c1d251`.

A4am does not complete N12. Broader Blocks/classic checkout matrix coverage, Reports reader-fee aggregation, account-state/browser/log/bundle breadth, harness checkout visual parity hardening, and the final accumulated A4 exit gate remained open at this checkpoint. Native admin readiness remains fail-closed.

### A4an Reports Balance Reader-Costs Baseline Addendum

A4an closes the Reports Balance row-contract gap where native carried the backend summary schema but did not render the reference `Reader costs` and related reconciliation rows. Native Reports Balance now renders the ordered reference row contract over the existing backend summary data: starting balance, charge captures, fee rows, refund rows, dispute rows, financing rows, payout fees, reader costs, network costs, other adjustments, net balance change, payouts, and ending balance.

The zero-activity baseline is corrected. Anchor rows remain visible with zero values, non-anchor rows render only when amount or count is non-zero, the empty-state copy now explains that zero summary rows are shown, and the live announcement no longer claims loaded rows when the period has no activity.

A4an evidence: focused Reports Jest passed with 20 tests across 3 suites; focused Reports REST PHPUnit passed with 9 tests and 43 assertions; exact-file ESLint, admin type lint, changed-file PHPCS, production PHPStan, admin bundle build, A4 admin surface gate, live Playwriter Reports proof, controlled Playwriter `Reader costs` proof, clean target debug/log scans, changelog validation, `git diff --check -- . ':!.agents'`, and branch lint passed. The slice is committed locally as source/tests `983ee15254` plus changelog `bdc86d6ea1`, with git range `d101c1d251...bdc86d6ea1`.

A4an does not complete N12. Broader checkout matrix coverage, account-state/browser/log/bundle breadth, harness checkout visual parity hardening, and the final accumulated A4 exit gate remained open at this checkpoint. Native admin readiness remains fail-closed.

### A4ao Blocks Express Checkout Element Baseline Addendum

A4ao closes the Blocks Express Checkout Element runtime gap. Native now registers a provider-owned Store API cart extension under `extensions.wcpay.express_checkout_methods`, consumes that cart-aware method contract in the split Blocks ECE bundle while retaining localized settings as an allowlist, restores mounted Stripe Elements options with `loader: 'never'`, appearance, locale, manual capture, and subscription setup-future-usage, and sends wallet click/shipping lifecycle data through Store API cart endpoints.

The checkout baseline now treats WooPay, Apple Pay / Google Pay, Link, Amazon Pay, and platform-backed express methods at the provider-level express-checkout abstraction. Stripe minor-unit handling is shared through `WooPaymentsCurrencyUtils`, including true zero-decimal currencies and special-case currencies such as `UGX`, and WooPay Blocks support features are forwarded from gateway settings without tying card rendering to WooPay. WooPayments ECE styling remains in the dedicated Blocks express checkout asset with scoped overflow-visible/focus rules.

A4ao evidence: combined focused PHP passed with 51 tests and 199 assertions; focused Blocks Jest for express checkout and WooPay passed with 26 tests; exact-file JS lint, targeted Stylelint, PHP syntax, changed-file PHPCS, production PHPStan, Blocks `build:project:bundle`, changelog validation, `git diff --check -- . ':!.agents'`, and branch lint passed. Playwriter proved target cart/checkout split assets, Stripe express iframe, WooPay checkout button, Store API `express_checkout_methods`, empty failed responses, empty console errors, and clean target logs. The slice is committed locally as source/tests `e34b3f96f2` plus changelog `c3b737269f`, with git range `bdc86d6ea1...c3b737269f`.

A4ao does not complete the final A4/N12 exit gate. Remaining reopened-A4 work is now the accumulated checkout/admin matrix, account-state/browser/log/bundle breadth, checkout visual-parity harness hardening, and the final fail-closed A4/N12 readiness decision. Native admin readiness remains fail-closed.

### A4ap Accumulated Checkout/Admin Parity Baseline Addendum

A4ap closes the next accumulated checkout/admin residual cluster after A4ao. Native protected admin routes now fail closed unless `adminRouteAvailability` explicitly permits the route, while Settings, Express Checkout settings, and Fraud Protection settings remain loadable through the Core Settings > Payments route seam. This preserves the N12 readiness contract that protected chunks and protected admin surfaces do not open before account-state authorization is known.

The checkout baseline is widened across card and WooPay without coupling those sibling payment abstractions. WooPay first-party auth now follows actual WooPay express availability and country eligibility; the split classic and Blocks WooPay bundles restore WooPay Connect preemptive sessions, preferred-card query/cache/rendering, OTP fallback, disabled/unavailable product-page preflight, `skip_woopay` cleanup, and scoped preferred-card button styling. Classic and Blocks card checkout now share the reference test-card copy fallback/success behavior, restore card-owned Stripe Elements wallet/Link/reusable-terms options, keep Blocks floating-label/appearance parity without the removed font-size clamp, and follow backend saved-card flags so WooPay save-my-info owns the save surface when appropriate.

The frontend asset baseline now uses reference-style card-brand artwork for WooPayments card checkout. Visa, Mastercard, Amex, Discover, and Cartes Bancaires use existing Core card-art assets, while JCB and UnionPay are added under a WooPayments-specific asset directory to avoid changing unrelated Core consumers of the existing generic JCB file. WooPayments card and WooPay styling remains in dedicated WooPayments bundles.

A4ap evidence: focused Blocks card Jest passed with 29 tests; focused classic card Jest passed with 21 tests; focused Blocks WooPay Jest passed with 8 tests; focused classic WooPay Jest passed with 11 tests; focused admin routes Jest passed with 31 tests; `WooPaymentsCheckoutBridgeTest` passed with 10 tests and 112 assertions; `WooPaymentsWooPaySessionServiceTest` passed with 27 tests and 121 assertions. Scoped Blocks/classic/admin ESLint, scoped Blocks stylelint, changed-file PHP lint, production PHPStan for touched WooPayments PHP classes, changelog validation, and `git diff --check -- . ':!.agents'` passed. Earlier A4ap bundle builds passed for Blocks, classic assets, and admin assets with only existing unrelated warnings.

Playwriter verified the target native checkout against the same-width reference: both use the compact one-row Stripe card field layout at that viewport; native renders the test-mode badge/instructions, copy button, mounted Stripe PaymentElement, reference-style Visa/Mastercard/Amex/Discover icons plus `+ 2` JCB/UnionPay overflow, subscription future-payment terms, and no standard saved-payment checkbox when WooPay save-my-info owns the save surface. WooPay express renders from the split WooPay bundle as a 48px purple button with `aria-label="WooPay"`. Target Docker logs and target `debug.log` had no PHP notices, warnings, deprecations, fatals, uncaught exceptions, stack traces, or actual 5xx markers in the proof window.

A4ap is committed locally as source/tests `746469525d` plus changelog `6cbb4314c2`, with git range `c3b737269f...6cbb4314c2`.

A4ap does not complete the final A4/N12 exit gate. Remaining work is the final accumulated admin/checkout matrix, account-state breadth, disabled-feature no-chunk/no-protected-REST checks, bundle/perf evidence at the boundary, and the explicit fail-closed readiness decision. Native admin readiness remains fail-closed.

### A4aq Final Accumulated Gate Baseline Addendum

A4aq adds the accumulated A4/N12 evidence gate that A4ap left open, but the gate result is intentionally not green yet. The ignored local harness now runs admin source/chunk, admin browser, checkout browser, bundle capture/compare, perf capture/compare, and log-scan checks into `data/a4aq/a4aq-accumulated-gate.json`.

The checkout/browser baseline is materially wider than previous per-slice proof. It covers target/reference, desktop/mobile, Blocks card checkout, Blocks checkout express, Blocks cart express, classic card checkout, My Account add-payment-method, and product express/WooPay surfaces. The gate asserts structural selectors, split WooPayments assets, Stripe iframe/resource presence, card test-mode/test-card/brand-overflow tokens, Store API `extensions.wcpay.express_checkout_methods`, and exact failed-response/console/page-error evidence. Checkout console warnings are fail-by-default unless they match explicit expected-warning metadata with source/count evidence. It does not claim 3DS/SCA, redirect, saved-method mutation, order-pay WooPay, pixel-perfect visual diff, or complete wallet-sheet behavior.

The admin browser baseline is also explicit about its current coverage gap. Available native routes are browser-checked, but six target protected route checks are classified as `incomplete` because the current connected account state makes those routes unavailable. Unavailable-route guard evidence is therefore not counted as full available-route merchant parity.

The bundle/log baseline is green under explicit limits. Bundle compare passes only with named A4aq budget dispositions, including a narrow `classic-card.css` growth allowance for restored checkout styling. Target log scanning passes with no target PHP/WP notices, warnings, deprecations, fatals, uncaught exceptions, database errors, stack traces, or actual 5xx markers. Reference-only local diagnostics are recorded as limitations, not target evidence.

The perf baseline remains incomplete. Gateway registration, hook count, controller count, autoload bytes, and `wcpay_account_data` autoload signals all compared favorably, but `process_payment`, `refund`, and `capture` perf probes require explicit local order fixtures and returned `requires_fixture` on both stores; REST route-registration status was also `preinitialized` on both stores. Per §0.6 invariant #4, this means A4aq cannot be treated as a fully exercised perf gate. Per N12, it also means no native admin readiness flip is authorized from A4aq.

Current conformance verdict: A4aq strengthens the accumulated boundary and records the first reusable admin/checkout/browser/bundle/log rollup, but the final A4/N12 baseline remains fail-closed and incomplete until the protected-route account-state coverage and money-path perf fixtures/probes are supplied or separately resolved by later source-backed gates. Final harness re-review approved the post-review hardening with critical 0, high 0, medium 0.

### A4ar A4aq Coverage Closure Baseline Addendum

A4ar closes the two A4aq coverage gaps without changing WooCommerce product code. The accumulated harness now supplies a target-only optional account-state scenario for Documents, Card Readers, and Capital protected admin routes; it snapshots and restores the target `wcpay_account_data` cache, records redacted before/after evidence, and runs the protected routes through Playwriter as available-route checks. The reference account is not mutated for this scenario because cache-only Capital flags cannot synthesize valid reference Capital loan platform data.

A4ar also closes the money-path perf fixture gap. The accumulated gate creates real local WooPayments process/refund/capture fixtures for both stores, validates their preconditions before capture, passes those order IDs into `perf-surface-gate.sh`, and keeps missing/invalid fixtures fail-closed. REST route-registration timing remains unavailable through local WP-CLI because both stores enter the probe after `rest_api_init`; the comparator now accepts that only as both-sides-preinitialized route snapshot evidence with payment-route and controller inventory present. Mixed or missing route evidence remains incomplete.

The checkout harness was hardened during A4ar closeout. Optional-admin Playwriter surface filters are reset before checkout so browser gates do not inherit each other's scoped state. Checkout required resource assertions now wait briefly for required asset evidence from rendered DOM URLs or performance entries before failing, which preserves real missing-logo failures while removing a reference-only race where the visible logo container appeared before the async `<img>` URLs were inserted.

A4ar evidence: syntax checks passed for changed Python/PHP/JS/shell harness files; negative WP-CLI validation rejects shell-control and remote transports; `data/a4ar-admin-smoke-3` passed optional admin coverage; `data/a4ar-perf-smoke-2` passed perf fixture/compare coverage; `data/a4ar-browser-smoke-1` and `data/a4ar-checkout-smoke-1` passed focused browser regressions; and initial full aggregate `data/a4ar-full-3/a4aq-accumulated-gate.json` passed with no failures and no incomplete checks. Review then found harness masking risks around optional-admin over-clearance, account restore exactness, WP eval-file timeout, perf provider-ID validation, and severe reference diagnostics. Those risks were fixed and covered by `test-a4ar-harness-regressions.py`.

Post-review evidence: `data/a4ar-reviewfix-perf-smoke-1` passed stricter fixture validation, `data/a4ar-reviewfix-browser-smoke-2` passed focused browser/log coverage, and full aggregate `data/a4ar-reviewfix-full-1/a4aq-accumulated-gate.json` passed with no failures and no incomplete checks. Code re-review then found that optional-admin restore could rewrite `wcpay_account_data` autoload before perf. The harness now snapshots and restores the option autoload column exactly, covered by a RED/GREEN regression. Full post-autoload-fix aggregate `data/a4ar-autoloadfix-full-1/a4aq-accumulated-gate.json` passed with no failures and no incomplete checks. The final post-autoload-fix run covered 57 base admin checks, 7 optional-admin checks, 24 checkout checks, bundle compare, perf compare, and log scan. Final stricter perf fixture orders were reference `592`/`593`/`594` and target `291`/`292`/`293` for process/refund/capture, with real provider `pi_` and `ch_` IDs on refund/capture preflights. Target and reference `wcpay_account_data` autoload values were `off` before and after the run, and target stayed `off` immediately after optional-admin restore before perf. Target logs were clean; the reference emitted five existing non-severe WooPayments plugin `credit_card_form` deprecations, recorded as a limitation.

Current conformance verdict: the previously incomplete A4aq optional-admin and perf fixture coverage is now green in the local accumulated harness. Reliability re-review and focused code re-review approved with no critical/high/medium findings after the autoload restore fix. Native admin readiness remains fail-closed because readiness is a separate explicit decision, not a side effect of this harness closure.

### A4as Admin Readiness Decision Addendum

A4as closes the separate readiness decision left open by A4ar. Decision-review confirmed `STAND`: after A4ar, the accumulated A4/N12 gate had no failures and no incomplete checks, so `WooPaymentsCutoverController::FILTER_NATIVE_ADMIN_SURFACES_READY` can default true as a product decision scoped only to native admin-surface readiness.

The implementation keeps the existing `woocommerce_woopayments_native_admin_surfaces_ready` filter as the fail-closed override. RED/GREEN coverage proves the post-A4ar default no longer reports `native_admin_surfaces_unavailable` when the other deterministic blockers are clear, and the explicit false override still reports that blocker and suppresses the soft cutover notice. The focused cutover controller suite passed with 37 tests and 91 assertions.

Local target preflight evidence in `data/a4as-cutover-state.json` reports native runtime ownership, `native_runtime_enabled: true`, `should_native_register: true`, empty `preflight_failures`, empty `failures`, and `ready: true`. `soft_notice` remains false in that evidence because the standalone WooPayments plugin is not active on the native target. Code review approved with no critical/high/medium findings.

Current conformance verdict: native admin-surface readiness is now green by default after the final A4/N12 accumulated gate closure. This does not enable mandatory auto-deactivation and does not relax the independent runtime, transport/provider, platform connection, provider-event, operational-queue, fee-remediation, or legacy Stripe Billing guards.

### A5f Post-A4as Cutover Rehearsal Baseline Addendum

A5f establishes the first post-A4as cutover handoff baseline over the current target runtime. The final rollup at `data/a5f-post-a4as-cutover/a5f-cutover-rehearsal.json` passed 27 phases with no failures and verifies that a clean target starts native-owned and cutover-ready, that default-off plugin reactivation returns runtime ownership to the standalone WooPayments plugin with the soft notice active, and that the real browser soft cutover disables the plugin and returns to native ownership.

The mandatory-cutover baseline remains opt-in. Under a temporary local mandatory helper, a real browser admin request auto-deactivates the standalone plugin and renders the native success notice, while a direct activation attempt is recorded as a passed expected-failure phase because the product activation guard blocks WooPayments with the core-included message. Under a synthetic preflight blocker, the real browser request renders the blocked notice, the standalone plugin stays active, and the state snapshot records `a5_synthetic_preflight_blocker` with `ready: false`.

The A5f platform/transport baseline re-runs the local WPCOM readiness script, owner user-token readiness probe, and WCPay V1 transport continuity probe in the same rehearsal window. The final target debug log scan was clean at 0 bytes, the target ended native-owned with WooPayments inactive, and the temporary A5f mandatory/blocker/trace MU helpers were absent.

Current conformance verdict: post-A4as soft and mandatory cutover handoff behavior is green for the local target state covered by A5f. This does not claim mandatory cutover is default-on, does not replace later A5 financial/data migration gates, and does not broaden beyond the local target/reference/browser/WPCOM readiness evidence recorded here.

### A5g Rollout Defaults And Multisite Runtime Gate Baseline Addendum

A5g establishes the explicit rollout-default baseline after A5f. The native runtime filter and mandatory cutover filter now receive named product defaults, both intentionally false. The focused tests prove those defaults remain fail-closed and that controlled gates can still opt in through the existing filters. This preserves the separation between current local cutover readiness and the later production default-on decision.

A5g also adds a local multisite runtime ownership gate. The state probe intentionally enables the native runtime for the request, then measures ownership under inactive WooPayments, per-site active WooPayments, and network-active WooPayments. The final rollup at `data/a5g-multisite-runtime/a5g-multisite-runtime-gate.json` passed with 28 phases, no failures, and no non-pass phases. It proves native ownership on sites without the standalone plugin, plugin ownership for the site or network where the standalone plugin is active, and return to native ownership after plugin deactivation.

The multisite gate was run in `existing-tests` mode because local disposable wp-env startup hit Docker phpMyAdmin layer registration failures. This mode resets and restores the WooCommerce tests wp-env only; it does not mutate the connected `:8889` target store, the `:8082` reference store, WPCOM, Stripe, account state, or transport state. Post-cleanup proof shows the tests wp-env restored to single-site with no WooPayments activation and no stale multisite constants in `wp-config.php`.

Current conformance verdict: A5g is green for rollout-default explicitness and multisite runtime ownership. It does not claim final mandatory production default-on readiness, WPCOM/account readiness, Stripe/account migration safety, canary/error/perf readiness, or A6 cleanup.

### A4bd Post-A4bc Accumulated Gate Refresh Addendum

A4bd refreshes the accumulated A4/N12 gate after the later A4av-A4bc settings parity slices landed. It changes no WooCommerce product code and does not replace the earlier A4as product decision that native admin-surface readiness defaults green after A4ar; instead, it verifies that the later settings parity work still composes with the accumulated admin, checkout, bundle, perf, and log baseline.

The final rollup at `data/a4bd-post-a4bc-accumulated-gate/a4aq-accumulated-gate.json` reports `status=pass`, zero failures, zero incomplete checks, and 13/13 checks passing across admin source, Playwriter state, base admin browser, optional-account admin browser, checkout browser, bundle reference/target/compare, perf fixtures/reference/target/compare, and log scan. Browser coverage includes 57 base admin checks, seven optional-admin target checks, and 24 checkout checks across target/reference plus desktop/mobile. Target logs remained clean and target `debug.log` stayed at the stale 112-byte automatic-update baseline.

The refresh preserves the same honest limitations rather than widening them into stronger claims. Optional Documents/Card Readers/Capital coverage remains target/synthetic-account reachability proof rather than full reference content parity; reference optional-route checks remain gated by local account/feature state; perf remains a coarse large-delta smoke gate, not exact latency proof; checkout browser coverage keeps explicit caveats around wallet-sheet/order-pay/setup-submission and hidden-input details; and the settings root tokens are representative while detailed A4av-A4bc settings parity remains backed by each focused slice's tests, browser proof, and review evidence.

Current conformance verdict: the post-A4bc accumulated refresh is green under the recorded limitations. It does not relax A5g's fail-closed rollout defaults, does not enable mandatory cutover, and does not claim final production rollout/canary/error/perf or A6 cleanup readiness.

### A5h Post-A4bd Cutover Readiness Refresh Addendum

A5h refreshes local A5 cutover readiness after A4bd without changing WooCommerce product code. It reruns the connected target-store cutover rehearsal and the isolated multisite runtime gate against the current post-A4bd branch, then reconciles the result against the explicit fail-closed rollout defaults.

The current A5f evidence at `data/a5h-post-a4bd-cutover-readiness/a5f-rerun-1/a5f-cutover-rehearsal.json` reports `status=pass`, `pass=true`, 27/27 phases passing, and no failures. It covers the native-owned baseline, plugin-owned default-off proof, real browser soft cutover, mandatory auto-deactivation only under a temporary helper, activation guard expected failure, synthetic preflight blocker path, native restore, local WPCOM readiness, owner-token readiness, WCPay V1 transport continuity, and a clean final target debug-log scan. The target ended native-owned with standalone WooPayments inactive and `debug.log` at 0 bytes.

The first A5h A5f run at `data/a5h-post-a4bd-cutover-readiness/a5f/a5f-cutover-rehearsal.json` remains diagnostic history. It failed only at the final target debug-log scan with one fresh `_load_textdomain_just_in_time` notice for the standalone `woocommerce-payments` plugin text domain while the plugin was active. Focused reproduction did not reproduce the notice, and the full rerun passed cleanly. The baseline therefore records the first run as a non-reproduced plugin-active diagnostic, not as a hidden pass.

The current A5g evidence at `data/a5h-post-a4bd-cutover-readiness/a5g/a5g-multisite-runtime-gate.json` reports `status=pass`, `pass=true`, 28/28 phases passing, and no failures in `existing-tests` mode. It proves per-site and network-active runtime ownership in the WooCommerce tests wp-env at `http://store8889.localhost:8087` and restores that tests env to single-site with no active plugins. It does not mutate the connected `:8889` target store, the `:8082` reference store, WPCOM, Stripe, account state, or transport state.

The source/default baseline remains fail-closed. `NativePaymentsRuntimeArbiter::DEFAULT_NATIVE_RUNTIME_ENABLED` is still `false`, `WooPaymentsCutoverController::DEFAULT_MANDATORY_CUTOVER_ENABLED` is still `false`, and focused tests continue to assert both defaults and filter override seams. Review `review-a5h-cutover-readiness-refresh.md` returned `PASS_WITH_LIMITATIONS` with no source-backed product blocker.

Current conformance verdict: A5h is green as a local A5 cutover readiness refresh under the recorded limitations. It does not claim production mandatory/default-on readiness, release rollout readiness, canary/error-rate readiness, WPCOM production readiness, exact perf readiness, or A6 cleanup readiness. A6 remains deferred because the old harness-deletion plan is stale and the current `tools/woopayments-merge` directory is ignored local verification infrastructure, not tracked product scaffolding to delete.

### A5i Local Readiness Decision Rollup Addendum

A5i records the post-A5h decision line recommended by `review-post-a5h-next-slice-scan.md`. The scan found no concrete source-backed local product or gate slice remaining before a production/default-on decision and recommended a decision rollup rather than another implementation slice.

Fresh sanity checks confirmed the evidence used by the rollup. A4bd's accumulated gate remains `status=pass` with 13/13 checks passing, zero failures, and zero incomplete checks. A5h's A5f rerun remains `status=pass` with 27 phases and no failures. A5h's A5g rerun remains `status=pass` with 28 phases, no failures, and `runtime_mode=existing-tests`. Source defaults remain fail-closed: native runtime default false, mandatory cutover default false, and `KNOWN_UNHANDLED_EVENT_TYPES` empty.

Current conformance verdict: the local A4/A5 readiness baseline is green under the recorded limitations. This is not a production-ready/default-on verdict. Production rollout/default-on, mandatory rollout, canary/error-rate gates, WPCOM production readiness, exact production perf, and release sequencing remain separate deferred fail-closed decisions. A6 cleanup remains deferred because the old A6 plan is stale and `tools/woopayments-merge` is ignored local verification infrastructure with zero tracked files.

### A5j Release/Default-On Readiness Packet Addendum

A5j refreshes the local release/default-on packet after A5i and the A6 boundary hold. It does not change WooCommerce product code or harness code. It classifies the release/default-on requirements, reruns the focused local A5 gates recommended by the gate-opportunity audit, and keeps the production/default-on decision fail-closed.

The source/default baseline remains unchanged and fail-closed. `NativePaymentsRuntimeArbiter::DEFAULT_NATIVE_RUNTIME_ENABLED` is still false, `WooPaymentsCutoverController::DEFAULT_MANDATORY_CUTOVER_ENABLED` is still false, and `WooPaymentsEventIngestor::KNOWN_UNHANDLED_EVENT_TYPES` is still empty. Focused guard PHPUnit passed with 123 tests and 391 assertions across `NativePaymentsRuntimeArbiterTest`, `WooPaymentsCutoverControllerTest`, and `WooPaymentsEventIngestorTest`. The fresh target probe at `data/a5j-target-cutover-state.json` reports native ownership, standalone WooPayments inactive, empty preflight failures, `ready: true`, and no failures.

`review-a5j-release-default-on-requirements.md` returned `BLOCKED_ON_PRODUCTION_DECISION`. It classifies local A4/A5 readiness and cutover mechanics as locally proven, but classifies canary parity, production error-rate, production WPCOM readiness, live queue/financial/Stripe Billing data safety, exact production perf, release sequencing, and the actual default/mandatory flip as production/release-only. `review-a5j-local-gate-opportunities.md` returned `RERUN_A5_ONLY`, recommending only A5f and A5g as useful local freshness gates.

The first A5j A5f run at `data/a5j-release-default-on-readiness/a5f/a5f-cutover-rehearsal.json` failed only at the final target debug-log scan with one fresh `_load_textdomain_just_in_time` notice for the standalone `woocommerce-payments` plugin text domain during the soft-cutover browser phase. That failure remains diagnostic history. The direct product path did not reproduce under the local trace helper: activation alone stayed clean, plugin-active `wp-admin/plugins.php` stayed clean, and isolated soft cutover stayed clean with an empty debug log. After closing 11 stale local target/reference admin tabs in the shared Playwriter context, removing the trace helper, and clearing the target log, the current A5f rerun at `data/a5j-release-default-on-readiness/a5f-rerun-1/a5f-cutover-rehearsal.json` passed with 27 phase results, no failures, target restored native-owned, standalone WooPayments inactive, and final target debug-log scan at 0 bytes.

The current A5g rerun at `data/a5j-release-default-on-readiness/a5g/a5g-multisite-runtime-gate.json` passed with 28 phase results, no failures, and `runtime_mode=existing-tests`. It proves local per-site and network-active runtime ownership in the WooCommerce tests wp-env and restores that env to single-site with no active plugins. The post-run restore probe returned `home=http://store8889.localhost:8087`, `is_multisite=false`, and `active_plugins=[]`.

Current conformance verdict: A5j is green as a refreshed local release/default-on packet under the recorded limitations. It does not claim production/default-on readiness, production WPCOM readiness, canary/error-rate readiness, exact production perf, live queue/financial/Stripe Billing data safety, release sequencing, or A6 cleanup readiness. A6 product cleanup remains held until the release/default-on decision is settled.

### A5k Next Local Slice Reconciliation Addendum

A5k tests whether the A5j stop line is a real production/release boundary or a missed local slice. It changes no WooCommerce product code and no harness code. Three fresh read-only audits were run in parallel and reconciled against current source and recorded evidence.

`review-a5k-release-sequencing-next-move.md` returned `PRODUCTION_DECISION_ONLY`. It found no source-backed local product, harness, or cleanup slice that should run before the production/default-on decision. A4/A5 local readiness, A5j A5f/A5g freshness, provider-event disposition, runtime mutual exclusion, cutover preflight mechanics, and the ignored harness disposition remain already-evidenced under recorded local limits. The remaining decisive items are production/release-only: canary parity/error-rate/perf, production WPCOM readiness, live queue/financial/Stripe Billing data safety, exact production perf, release sequencing, and the actual default/mandatory flips.

`review-a5k-n12-admin-parity-residuals.md` returned `NO_LOCAL_A4_BLOCKER`. Current source exposes persistent native WooPayments admin navigation under the Core Payments parent and canonical Settings > Payments `/woopayments/*` provider routes, gated by native runtime ownership, `manage_woocommerce`, account state, and optional-surface predicates. It found no orphaned-route gap and no concrete untracked local A4 parity slice after A4av-A4bd/A5j. This preserves A4bd's honest limitations: the accumulated browser/token gate is not unlimited visual proof for every optional/account-state route, but there is no source-backed local admin parity blocker left to reopen. `FILTER_NATIVE_ADMIN_SURFACES_READY` defaulting ready remains defensible as an admin-readiness decision; explicit false still blocks preflight, while runtime rollout and mandatory cutover defaults remain fail-closed.

`review-a5k-a6-cleanup-boundary.md` returned `HOLD_A6_FOR_RELEASE`. It confirms A6a remains a valid future cleanup slice, but not before release/default-on sequencing. The current Core settings surface for `_wcpay_feature_subscriptions` mirrors the standalone WooPayments extension's disable-only contract, and the unmodified extension still uses that option to load `subscriptions-core` / bundled subscriptions behavior when it owns runtime. Since plugin rollback/coexistence remains part of the fail-closed A5 safety model, removing the Core settings API/UI now would change merchant-facing behavior and rollback behavior. A6a is therefore release-sequenced, not discarded.

Current conformance verdict: A5k records a source-backed local stop line. The branch remains locally green and fail-closed under the recorded limitations, but the next unblocker is production/release-owner evidence and decisioning. Do not start A6 product cleanup or rerun local gates as a substitute for canary/production/default-on evidence unless new source changes land or a release owner explicitly requests a fresh local timestamp.

### A5l Blocked Audit Addendum

A5l audits whether the active long-running goal has reached the strict blocked threshold. It changes no WooCommerce product code and no harness code. `review-a5l-blocker-audit.md` returned `BLOCKED_THRESHOLD_MET` and found no failed claims.

The repeated blocker is source-backed and artifact-backed. A5i found no concrete source-backed local product or gate slice remained before a production/default-on release decision. A6 held cleanup for release/default-on sequencing. A5j refreshed the useful local A5 evidence and still returned `BLOCKED_ON_PRODUCTION_DECISION`. A5k challenged release sequencing, N12/admin parity, and A6 cleanup, returning `PRODUCTION_DECISION_ONLY`, `NO_LOCAL_A4_BLOCKER`, and `HOLD_A6_FOR_RELEASE`. A5l verifies that this is the same blocker across at least three consecutive substantive continuations, not a new local defect.

The exact blocker is missing production/release-owner evidence and decisioning for canary parity/error-rate/perf, production WPCOM readiness, live queue/financial/Stripe Billing data safety, exact production perf, release sequencing, and the actual native runtime/default/mandatory flips. A6a cleanup remains blocked until release/default-on sequencing clears the plugin-active rollback/coexistence window or an explicit release-owner decision changes that boundary.

Current conformance verdict: the branch remains locally green and fail-closed under the recorded limitations, but the full objective is blocked, not complete. No local product, harness, docs, or evidence slice remains that would materially advance the full objective without production/default-on evidence; additional local reruns would add recency only, and starting A6a would violate the held rollback/coexistence boundary. Post-close hygiene outside `.agents` is clean: branch-ahead only, empty product diff stat, and `git diff --check` clean.
